{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://modforge/spec.schema.json",
  "title": "ModForge plugin spec",
  "description": "Describes the content of one Skyrim plugin. See docs/SPEC.md. Property names are matched case-insensitively by the generator.",
  "$comment": "In a spec DATA file, an object { \"$ref\": ... } or { \"$env\": ... } is a ModForge preprocessor directive (see docs/SPEC-refs.md) — $ref splices JSON from a file / file#pointer / same-doc pointer, $env substitutes an environment variable. They are resolved BEFORE deserialization, may appear in place of any value, and never reach the record builder, so this schema does not (and cannot) validate them. This is unrelated to the JSON-Schema $ref used internally within this schema document.",
  "type": "object",
  "properties": {
    "pluginName": {
      "type": "string",
      "description": "Output filename / ModKey, e.g. MyMod.esp"
    },
    "esl": {
      "type": "boolean",
      "default": true,
      "description": "Light-master flag; keep new records <= 4096"
    },
    "miscItems": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/named"
      }
    },
    "books": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/book"
      }
    },
    "weapons": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/weapon"
      }
    },
    "npcs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/npc"
      }
    },
    "quests": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/quest"
      }
    },
    "dialogue": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/dialogue"
      }
    },
    "banter": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/banter"
      },
      "description": "Banter: proactive UNPROMPTED NPC lines (the follower-comment pattern); entries sharing a (speaker,quest) become one Random-pick ambient IDLE topic"
    },
    "scenes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/scene"
      },
      "description": "Scene (SCEN): two+ NPCs talking to EACH OTHER (not the player), hosted by a quest, played as ordered phases; supports presence-gated autoStart + non-dialog (package/timer) beats"
    },
    "relationships": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/relationship"
      },
      "description": "Relationship (RELA): a directed NPC_-to-NPC_ bond at a rank; child defaults to the player NPC base 0x000014"
    },
    "combatStyles": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/combatStyle"
      },
      "description": "CombatStyle (CSTY): HOW an NPC fights; reference via npc.combatStyle"
    },
    "packages": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/package"
      },
      "description": "AI Package (PACK); assign via npc.packages. See SPEC.md for the template-input subobjects"
    },
    "npcPatches": {
      "type": "array",
      "description": "override an EXISTING vanilla/other-master NPC and swap its AI package list (AI Overhaul style). Needs the master (MODFORGE_SKYRIM_DATA); the override carries the NPC's real English name inline.",
      "items": {
        "type": "object",
        "required": [
          "overrideOf",
          "packages"
        ],
        "additionalProperties": false,
        "properties": {
          "overrideOf": {
            "type": "string",
            "description": "the existing NPC ref '<master>:0xFORMID' (e.g. Skyrim.esm:0x013B99 = Carlotta Valentia)"
          },
          "packages": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "PACK refs (in-spec editorId or vanilla '<master>:0xFORMID') forming the new schedule"
          },
          "mode": {
            "enum": [
              "replace",
              "prepend",
              "append"
            ],
            "description": "replace = use ONLY our packages; prepend/append = keep the NPC's existing packages and add ours before/after (default replace)"
          }
        }
      }
    },
    "spells": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/spell"
      }
    },
    "magicEffects": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/magicEffect"
      },
      "description": "custom MGEF records an effect can point at by editorId"
    },
    "projectiles": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/projectile"
      },
      "description": "Projectile (PROJ): the flying bolt of an Aimed spell; a magicEffect.projectile ref points here"
    },
    "explosions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/explosion"
      },
      "description": "Explosion (EXPL): the impact AoE a projectile/spell triggers; a magicEffect.explosion or projectile.explosion ref points here"
    },
    "hazards": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/hazard"
      },
      "description": "Hazard (HAZD): a radius effect applying a spell to actors in it; spell-spawned (MGEF archetype SpawnHazard) or placed (PlacedHazard)"
    },
    "musicTracks": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/musicTrack"
      },
      "description": "Music Track (MUST): an audio entry (SingleTrack/Palette/SilentTrack) referenced by a music (MUSC)"
    },
    "music": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/music"
      },
      "description": "Music Type (MUSC): a container assigned to cells[].music / worldspaces[].music"
    },
    "imageSpaceModifiers": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/imageSpaceModifier"
      },
      "description": "ImageSpaceModifier (IMAD): a screen-space post-process (brightness/tint) applied at runtime; referenced by explosion.imageSpaceModifier or a Papyrus property"
    },
    "potions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/potion"
      }
    },
    "armors": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/armor"
      }
    },
    "factions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/faction"
      },
      "description": "Faction (FACT); add a `vendor` sub-object to make members shopkeepers"
    },
    "classes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/class"
      },
      "description": "Class (CLAS): an npc 'profession' (attribute/skill weights); an npc class can point at one"
    },
    "messages": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/message"
      }
    },
    "scripts": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/script"
      }
    },
    "cells": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/cell"
      },
      "description": "new interior cells (reach in-game via `coc <editorId>`)"
    },
    "placements": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/placement"
      },
      "description": "put a base form into a cell at a position/rotation"
    },
    "mapMarkers": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/mapMarker"
      },
      "description": "permanent world-map markers (XMRK on the MapMarker static)"
    },
    "leveledItems": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/leveledList"
      },
      "description": "LeveledItem (LVLI): level-gated weighted item lists"
    },
    "leveledNpcs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/leveledList"
      },
      "description": "LeveledNpc (LVLN): level-gated weighted npc lists (also a placement `base` for a leveled-actor spawn)"
    },
    "formLists": {
      "type": "array",
      "description": "FormList (FLST): an ordered list of FormIDs of any type; use as a list-taking condition's param (GetItemCount/GetEquipped/GetIsVoiceType/GetInWorldspace), a keyword/clothing set, etc.",
      "items": {
        "type": "object",
        "required": [
          "editorId"
        ],
        "additionalProperties": false,
        "properties": {
          "editorId": {
            "type": "string"
          },
          "items": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "refs (in-spec editorId or vanilla <master>:0xFORMID), any record type; order preserved"
          }
        }
      }
    },
    "encounterZones": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/encounterZone"
      },
      "description": "EncounterZone (ECZN): level scaling / respawn for an area; a cell/spawn `encounterZone` points at one"
    },
    "containers": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/container"
      },
      "description": "Container (CONT): a named container with item contents"
    },
    "ingredients": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ingredient"
      },
      "description": "Ingredient (INGR): alchemy reagent with effects"
    },
    "ammunitions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ammunition"
      },
      "description": "Ammunition (AMMO): arrow/bolt with damage"
    },
    "scrolls": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/scroll"
      },
      "description": "Scroll (SCRL): one-shot spell-as-item with effects + cast fields"
    },
    "soulGems": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/soulGem"
      },
      "description": "SoulGem (SLGM)"
    },
    "keys": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/key"
      },
      "description": "Key (KEYM)"
    },
    "keywords": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/keyword"
      },
      "description": "Keyword (KYWD): define your own so in-spec records can reference it"
    },
    "outfits": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/outfit"
      },
      "description": "Outfit (OTFT): a set of item refs an NPC can wear"
    },
    "statics": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/staticRec"
      },
      "description": "Static (STAT): a world mesh (model path)"
    },
    "activators": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/activator"
      },
      "description": "Activator (ACTI): an interactable world object"
    },
    "furniture": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/furniture"
      },
      "description": "Furniture (FURN): a placeable interactive object (chair/bed/bench), vanilla or custom mesh"
    },
    "sounds": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/sound"
      },
      "description": "SoundDescriptor (SNDR): wraps a user .wav/.xwm so records can FormLink to it"
    },
    "assets": {
      "type": "string",
      "description": "External-resource source dir; package copies its Meshes/Textures/Sounds/Music/Seq sub-trees next to the .esp. See docs/external_assets.md"
    },
    "textureSets": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/textureSet"
      },
      "description": "TextureSet (TXST): retexture an existing mesh without a new .nif"
    },
    "recipes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/recipe"
      },
      "description": "ConstructibleObject (COBJ): crafting recipes"
    },
    "enchantments": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/enchantment"
      },
      "description": "Enchantment / Object Effect (ENCH): MGEF-based effects bundled into a weapon/staff/apparel enchantment a weapon/armor references via its `enchantment` field"
    },
    "lights": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/light"
      },
      "description": "Light (LIGT): a custom light source (colour/radius/flicker); place it via placements[] by editorId"
    },
    "lightingTemplates": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/lightingTemplate"
      },
      "description": "LightingTemplate (LGTM): reusable interior lighting (ambient/directional/fog + DALC hemisphere). Author by copying a vanilla LGTM via `template` then overriding fields. CELLs reference it via cells[].lightingTemplate"
    },
    "imageSpaces": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/imageSpace"
      },
      "description": "ImageSpace (IMGS): screen-space HDR/cinematic/tint attached to a CELL. Copy a vanilla IMGS via `template` then bump brightness/saturation etc. CELLs reference it via cells[].imageSpace"
    },
    "weathers": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/weather"
      },
      "description": "Weather (WTHR): a custom sky (cloud layers, per-time-of-day colours, precipitation, wind, fog)"
    },
    "climates": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/climate"
      },
      "description": "Climate (CLMT): a weather cycle + sunrise/sunset timing + sun/moon textures; references weathers"
    },
    "worldspaces": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/worldspace"
      },
      "description": "Worldspace (WRLD): a new exterior world (climate/water/parent/map). RECORD LAYER ONLY — terrain/LOD/navmesh stay CK-side."
    },
    "regions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/region"
      },
      "description": "Region (REGN): an area inside a worldspace + its weather table (the climate hook)."
    },
    "perks": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/perk"
      },
      "description": "Perk (PERK): a passive ability / stat-or-combat modifier; attach to an NPC via npc.perks"
    },
    "wordsOfPower": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/wordOfPower"
      },
      "description": "WordOfPower (WOOP): one dragon-language syllable; referenced by a shout's words[].word + a word wall's word"
    },
    "shouts": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/shout"
      },
      "description": "Shout (SHOU): a dragon shout = up to 3 words, each a WOOP + a Voice SPEL. Unusable until learned at a word wall"
    },
    "wordWalls": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/wordWall"
      },
      "description": "The learnable layer: a teaching quest + generated Papyrus fragment + WordWallTrigger placement that teaches a shout's word"
    },
    "globals": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/global"
      },
      "description": "GlobalVariable (GLOB): a named number shared game-wide, save-persisted, readable by conditions (GetGlobalValue) + Papyrus. `value` is the INITIAL value only (existing saves keep their runtime value)"
    },
    "identities": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/identity"
      },
      "description": "Lightweight identity/class system: each identity is a FACT (held signal) + priority + optional grants/acquire. Gate dialogue with identity/primaryIdentity; acquire via a book's OnRead."
    },
    "presets": {
      "$ref": "#/$defs/presetCatalog",
      "description": "Non-emitting cookbook catalog. Named fragments under lighting/weather/packages/identities are ignored by build; copy or expand them into the normal top-level arrays to emit records."
    },
    "voiceTemplates": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/voiceTemplate"
      },
      "description": "named voice recipes for cloning (Spec.Voice.cs)"
    },
    "voiceSpeakers": {
      "type": "array",
      "description": "bind an EXTERNAL dialogue speaker (an NPC from another master the mod-only cache can't resolve, e.g. an existing follower) to a voiceType + voiceTemplate, so voicelines/voicediag can voice lines gated on GetIsID(<that NPC>)",
      "items": {
        "type": "object",
        "required": [
          "speaker",
          "voiceType",
          "template"
        ],
        "additionalProperties": false,
        "properties": {
          "speaker": {
            "type": "string",
            "description": "the NPC ref the line's GetIsID gates on (<master>:0xFORMID)"
          },
          "voiceType": {
            "type": "string",
            "description": "that NPC's voiceType (the Sound/Voice/<plugin>/ folder name, e.g. JJSofiaVoiceType)"
          },
          "template": {
            "type": "string",
            "description": "a voiceTemplates[] id to generate the lines with"
          }
        }
      }
    },
    "voiceLine": {
      "$ref": "#/$defs/voiceLine",
      "description": "global voice output settings"
    },
    "animationReplacers": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/animationReplacer"
      },
      "description": "OAR (Open Animation Replacer) replacer/moveset folders generated as LOOSE FILES (no esp). Each is a replacer-mod with named submods; .hkx animations are user-supplied. See docs/spec/SPEC-animation.md."
    },
    "behaviorData": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/behaviorData"
      },
      "description": "Behavior Data Injector (BDI) configs: inject graph variables/events into a behavior project with no behavior patch. Emitted to SKSE/Plugins/BehaviorDataInjector/<file>.json."
    },
    "payloadMacros": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/payloadMacro"
      },
      "description": "Payload Interpreter (PIE) macro tables: named macros -> payload commands. Emitted to SKSE/PayloadInterpreter/Config/<file>.ini."
    },
    "spidDistributions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/spidDistribution"
      },
      "description": "SPID (Spell Perk Item Distributor) configs: attach records to NPC actorbases by filter with no ESP patch. Emitted as a loose <file>_DISTR.ini at the mod folder root (= Data/). See docs/spec/SPEC-distribution.md."
    },
    "mcmConfigs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/mcmConfig"
      },
      "description": "MCM Helper settings menus: emitted as loose MCM/Config/<modName>/config.json + settings.ini (no ESP record, no Papyrus for the ModSetting* ini-backed path). Requires MCM Helper + SkyUI at runtime. See docs/spec/SPEC-distribution.md."
    },
    "formListInjects": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/formListInject"
      },
      "description": "FormList Manipulator (FLM) configs: append forms to any already-loaded FormList at runtime with no ESP override (zero conflict). Emitted as a loose <file>_FLM.ini at the mod folder root (= Data/). Requires FormListManipulator.dll at runtime. See docs/spec/SPEC-distribution.md."
    },
    "kidDistributions": {
      "type": "array",
      "items": { "$ref": "#/$defs/kidDistribution" },
      "description": "KID (Keyword Item Distributor) configs: attach a Keyword to matching records by filter, no ESP patch. Emitted as a loose <file>_KID.ini at the mod root. Requires KID.dll. See docs/spec/SPEC-distribution.md."
    },
    "objectSwaps": {
      "type": "array",
      "items": { "$ref": "#/$defs/objectSwap" },
      "description": "Base Object Swapper (BOS) configs: replace a base object with another when a reference loads, no ESP override. Emitted as a loose <file>_SWAP.ini at the mod root. Requires po3_BaseObjectSwapper.dll. See docs/spec/SPEC-distribution.md."
    },
    "animObjectSwaps": {
      "type": "array",
      "items": { "$ref": "#/$defs/animObjectSwap" },
      "description": "AnimObject Swapper (AOS) configs: swap the prop an actor holds during an idle, no ESP override. Emitted as a loose <file>_ANIO.ini at the mod root. Requires AnimObjectSwapper.dll. See docs/spec/SPEC-distribution.md."
    },
    "skyPatchers": {
      "type": "array",
      "items": { "$ref": "#/$defs/skyPatcher" },
      "description": "SkyPatcher configs: in-memory runtime record edits by filter, no ESP override. Emitted as a loose SKSE/Plugins/SkyPatcher/<recordType>/<file>.ini. Requires SkyPatcher.dll. See docs/spec/SPEC-distribution.md."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "ref": {
      "type": "string",
      "minLength": 1,
      "description": "A reference: an in-spec editorId OR an external form '<master>:0xFORMID' (e.g. 'Skyrim.esm:0x013746' = NordRace). Discover FormIDs with the `find` command."
    },
    "persistKey": {
      "type": "string",
      "default": "speaker",
      "description": "the Form the state hangs on. Tokens: 'speaker' (the dialogue NPC, akSpeakerRef — DIALOGUE LINES ONLY) or 'player' (Game.GetPlayer()). Any other value is an arbitrary ref (editorId or '<master>:0xFORMID') bound as a Form property. A quest STAGE has no speaker → use 'player' or a ref."
    },
    "persist": {
      "type": "object",
      "description": "JContainers JFormDB: write nested per-Form state when this hook fires (Idea #20 in-world skill tree). On a dialogue line it runs in the TIF fragment when the line is picked; on a quest stage it runs when the quest reaches that stage. Emits JFormDB.solveXxxSetter — root-DB path API only, so no retain/release lifecycle. Compiling needs JContainers' .psc on the header path.",
      "required": [
        "storage",
        "set"
      ],
      "additionalProperties": false,
      "properties": {
        "storage": {
          "type": "string",
          "description": "JFormDB storageName (the namespace bucket; first path component). One storage holds every keyed Form's entry."
        },
        "key": { "$ref": "#/$defs/persistKey" },
        "set": {
          "type": "array",
          "description": "the writes performed, in order",
          "items": {
            "type": "object",
            "required": [
              "path"
            ],
            "additionalProperties": false,
            "properties": {
              "path": {
                "type": "string",
                "description": "subpath under the storage, e.g. '.Endurance.nodes.Adaptation' (concatenated after '.<storage>')"
              },
              "int": { "type": "integer", "description": "int value (solveIntSetter)" },
              "float": { "type": "number", "description": "float value (solveFltSetter)" },
              "str": { "type": "string", "description": "string value (solveStrSetter)" },
              "form": { "$ref": "#/$defs/ref", "description": "Form value (solveFormSetter), bound as a VMAD property" },
              "delta": { "type": "boolean", "description": "int/float only: add to the current stored value (read-add-write) instead of replacing — for counters like accumulating XP/ratio" }
            }
          }
        }
      }
    },
    "syncPerks": {
      "type": "object",
      "description": "JContainers: AddPerk/RemovePerk on the key actor from its stored JFormDB node ranks when this hook fires (idempotent; runs after persist). Idea #20 SyncPerks — an actor's perks track its stored skill state.",
      "required": [
        "storage",
        "nodes"
      ],
      "additionalProperties": false,
      "properties": {
        "storage": {
          "type": "string",
          "description": "JFormDB storageName (same bucket as the persist writes)"
        },
        "key": { "$ref": "#/$defs/persistKey" },
        "nodes": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "path",
              "perk"
            ],
            "additionalProperties": false,
            "properties": {
              "path": { "type": "string", "description": "subpath to the node's stored rank int (read with solveInt)" },
              "perk": { "$ref": "#/$defs/ref", "description": "PERK added when stored rank >= minRank, removed below" },
              "minRank": { "type": "integer", "default": 1, "description": "stored rank at/above which the perk is added (default 1)" }
            }
          }
        }
      }
    },
    "presetCatalog": {
      "type": "object",
      "description": "Named cookbook fragments grouped by existing spec family. This is documentation/data only: the generator does not emit records from presets directly.",
      "properties": {
        "lighting": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/presetFragment"
          },
          "description": "Interior lighting fragments, usually lightingTemplates/imageSpaces/lights/cell fields."
        },
        "weather": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/presetFragment"
          },
          "description": "Outdoor weather fragments, usually imageSpaces/weathers/climates/regions."
        },
        "packages": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/presetFragment"
          },
          "description": "AI package fragments using existing PackageSpec shapes."
        },
        "identities": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/presetFragment"
          },
          "description": "Identity fragments using existing IdentitySpec plus optional books/perks/spells."
        }
      },
      "additionalProperties": false
    },
    "presetFragment": {
      "type": "object",
      "description": "A named cookbook fragment. `_` may describe intent; other properties mirror existing top-level spec arrays."
    },
    "effect": {
      "type": "object",
      "required": [
        "magicEffect"
      ],
      "properties": {
        "magicEffect": {
          "$ref": "#/$defs/ref",
          "description": "MagicEffect ref — a vanilla one (e.g. 'Skyrim.esm:0x03EB15' = AlchRestoreHealth; find <Skyrim.esm> <query> MagicEffect) OR an in-spec magicEffects entry's editorId"
        },
        "magnitude": {
          "type": "number",
          "minimum": 0
        },
        "area": {
          "type": "integer",
          "minimum": 0
        },
        "duration": {
          "type": "integer",
          "minimum": 0,
          "description": "seconds; 0 = instant"
        }
      }
    },
    "weapon": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "damage": {
          "type": "integer",
          "minimum": 0,
          "description": "base damage; functional weapon needs this > 0"
        },
        "speed": {
          "type": "number",
          "minimum": 0,
          "description": "swing speed (~1.0 normal); defaults to 1.0 if any stat is set"
        },
        "reach": {
          "type": "number",
          "minimum": 0,
          "description": "reach (~1.0 normal); defaults to 1.0 if any stat is set"
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        },
        "enchantment": {
          "$ref": "#/$defs/ref",
          "description": "ENCH ref — an in-spec enchantments entry or a vanilla ObjectEffect (<master>:0xFORMID)"
        },
        "enchantmentAmount": {
          "type": "integer",
          "minimum": 0,
          "description": "the weapon's charge pool (casts before recharge); e.g. 1500-3000; 0 = engine auto-calc"
        },
        "template": {
          "type": "string",
          "description": "vanilla weapon to clone for model/animation/sounds (<master>:0xFORMID). A bare WEAP with no template + no model equips INVISIBLE. Cloning also keeps the template's stats for any field left at 0 (e.g. leave `damage` 0 to inherit)"
        },
        "model": {
          "type": "string",
          "description": "optional .nif path (rooted at Meshes\\, omit the prefix); overrides the cloned template's model"
        },
        "pickUpSound": {
          "$ref": "#/$defs/ref",
          "description": "SNDR ref played when picked up"
        },
        "putDownSound": {
          "$ref": "#/$defs/ref",
          "description": "SNDR ref played when dropped"
        }
      }
    },
    "enchantment": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "enchantType": {
          "enum": [
            "weapon",
            "apparel",
            "armor",
            "staff"
          ],
          "default": "weapon",
          "description": "weapon = cast-on-strike (charge); apparel = constant-effect (always-on while worn); staff = staff cast-on-use (charge)"
        },
        "castType": {
          "enum": [
            "ConstantEffect",
            "FireAndForget",
            "Concentration"
          ],
          "description": "optional — overrides the per-enchantType default"
        },
        "targetType": {
          "enum": [
            "Self",
            "Touch",
            "Aimed",
            "TargetActor",
            "TargetLocation"
          ],
          "description": "optional — overrides the per-enchantType default"
        },
        "enchantmentCost": {
          "type": "integer",
          "minimum": 0,
          "description": "per-cast charge cost (weapon/staff) / worn cost (apparel)"
        },
        "chargeTime": {
          "type": "number",
          "minimum": 0,
          "description": "staff cast charge-up (vanilla staves ~0.5)"
        },
        "effects": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/effect"
          },
          "description": "MGEF-based effects (needs at least one)"
        }
      }
    },
    "spell": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "effects": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/effect"
          },
          "description": "magic effects (does nothing without at least one)"
        },
        "spellType": {
          "enum": [
            "Spell",
            "Disease",
            "Power",
            "LesserPower",
            "Ability",
            "Poison",
            "Addiction",
            "Voice"
          ]
        },
        "castType": {
          "enum": [
            "ConstantEffect",
            "FireAndForget",
            "Concentration"
          ]
        },
        "targetType": {
          "enum": [
            "Self",
            "Touch",
            "Aimed",
            "TargetActor",
            "TargetLocation"
          ]
        },
        "baseCost": {
          "type": "integer",
          "minimum": 0,
          "description": "magicka cost"
        },
        "chargeTime": {
          "type": "number",
          "minimum": 0
        },
        "equipType": {
          "$ref": "#/$defs/ref",
          "description": "EQUP ref — which slot the spell occupies. Castable types (Spell/Voice/Power/LesserPower) default to EitherHand (Skyrim.esm:0x013F44) when omitted; set only to override (BothHands/Left/RightHand). A hand spell with no EQUP can't be equipped; a shout word-spell needs one or the player can't shout it"
        }
      }
    },
    "magicEffect": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "a custom MGEF; reference it from an effect's magicEffect by editorId",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "archetype": {
          "enum": [
            "ValueModifier",
            "Script",
            "Dispel",
            "CureDisease",
            "Absorb",
            "DualValueModifier",
            "Calm",
            "Demoralize",
            "Frenzy",
            "Disarm",
            "CommandSummoned",
            "Invisibility",
            "Light",
            "Lock",
            "Open",
            "Bound",
            "SummonCreature",
            "DetectLife",
            "Telekinesis",
            "Paralysis",
            "Reanimate",
            "SoulTrap",
            "TurnUndead",
            "Guide",
            "WerewolfFeed",
            "CureParalysis",
            "CureAddiction",
            "CurePoison",
            "Concussion",
            "ValueAndParts",
            "AccumulateMagnitude",
            "Stagger",
            "PeakValueModifier",
            "Cloak",
            "Werewolf",
            "SlowTime",
            "Rally",
            "EnhanceWeapon",
            "SpawnHazard",
            "Etherealize",
            "Banish",
            "SpawnScriptedRef",
            "Disguise",
            "GrabActor",
            "VampireLord"
          ],
          "description": "what the effect does; ValueModifier is the common damage/heal/fortify"
        },
        "actorValue": {
          "type": "string",
          "description": "the actor value it acts on, e.g. Health, Magicka, Stamina, MagickaRate"
        },
        "secondActorValue": {
          "type": "string",
          "description": "DualValueModifier archetype only: a 2nd affected actor value (e.g. Magicka)"
        },
        "secondActorValueWeight": {
          "type": "number",
          "description": "DualValueModifier archetype only: how the magnitude splits to the 2nd AV (0 = all to primary)"
        },
        "magicSkill": {
          "type": "string",
          "description": "school: Alteration|Conjuration|Destruction|Illusion|Restoration (or None)"
        },
        "resistValue": {
          "type": "string",
          "description": "actor value that resists it, e.g. ResistFire, ResistFrost, PoisonResist (or None)"
        },
        "castType": {
          "enum": [
            "ConstantEffect",
            "FireAndForget",
            "Concentration"
          ]
        },
        "targetType": {
          "enum": [
            "Self",
            "Touch",
            "Aimed",
            "TargetActor",
            "TargetLocation"
          ]
        },
        "baseCost": {
          "type": "number",
          "minimum": 0
        },
        "flags": {
          "type": "array",
          "items": {
            "enum": [
              "Hostile",
              "Recover",
              "Detrimental",
              "SnapToNavmesh",
              "NoHitEvent",
              "DispelWithKeywords",
              "NoDuration",
              "NoMagnitude",
              "NoArea",
              "FXPersist",
              "GoryVisuals",
              "HideInUI",
              "NoRecast",
              "PowerAffectsMagnitude",
              "PowerAffectsDuration",
              "Painless",
              "NoHitEffect",
              "NoDeathDispel"
            ]
          },
          "description": "instant restore/damage: [NoDuration,NoArea] (+Hostile,Detrimental for damage); timed fortify: [Recover]. Recover on an instant effect cancels it."
        },
        "association": {
          "$ref": "#/$defs/ref",
          "description": "summoned/bound form ref (only for Summon/Bound-type archetypes)"
        },
        "projectile": {
          "$ref": "#/$defs/ref",
          "description": "PROJ ref — the bolt that travels; needed for an Aimed spell to be visible (e.g. Skyrim.esm:0x10FBEA)"
        },
        "castingArt": {
          "$ref": "#/$defs/ref",
          "description": "ARTO ref — FX at the caster's hands (e.g. Skyrim.esm:0x01B211)"
        },
        "hitEffectArt": {
          "$ref": "#/$defs/ref",
          "description": "ARTO ref — FX at the impact point"
        },
        "explosion": {
          "$ref": "#/$defs/ref",
          "description": "EXPL ref — AoE explosion on impact"
        },
        "sounds": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "type": {
                "enum": [
                  "Release",
                  "Charge",
                  "Ready",
                  "SheathDraw",
                  "ConcentrationCastLoop",
                  "OnHit"
                ],
                "default": "Release"
              },
              "sound": {
                "type": "string"
              }
            }
          },
          "description": "MGEF sound descriptors; Release = the Thuum voice the player yells for a shout"
        }
      }
    },
    "potion": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "effects": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/effect"
          },
          "description": "magic effects (a potion is fully functional with one)"
        },
        "template": {
          "type": "string",
          "description": "vanilla ALCH to clone a model from (<master>:0xFORMID)"
        }
      }
    },
    "named": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          },
          "description": "keyword refs (weapons/misc; ignored on records that don't take keywords)"
        },
        "template": {
          "type": "string",
          "description": "(miscItems) vanilla MISC to clone a model from (<master>:0xFORMID)"
        },
        "model": {
          "type": "string",
          "description": "(miscItems) .nif path (rooted at Meshes\\, omit the prefix)"
        },
        "pickUpSound": {
          "$ref": "#/$defs/ref",
          "description": "(miscItems) SNDR ref played when picked up"
        },
        "putDownSound": {
          "$ref": "#/$defs/ref",
          "description": "(miscItems) SNDR ref played when dropped"
        }
      }
    },
    "faction": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "vendor": {
          "type": "object",
          "description": "Vendor data (FACT VENV/VEND/VENC). Present => Vendor-flagged merchant faction; members are shopkeepers and the generic 'I'd like to trade' topic surfaces.",
          "properties": {
            "startHour": {
              "type": "integer",
              "minimum": 0,
              "maximum": 24,
              "description": "shop opens (0..24)"
            },
            "endHour": {
              "type": "integer",
              "minimum": 0,
              "maximum": 24,
              "description": "shop closes (0..24); must be > startHour"
            },
            "radius": {
              "type": "integer",
              "minimum": 0,
              "description": "sell radius (0 = engine default)"
            },
            "buysStolen": {
              "type": "boolean",
              "description": "OnlyBuysStolenItems — a fence"
            },
            "sellBuyList": {
              "$ref": "#/$defs/ref",
              "description": "FormList of VendorItem keywords (e.g. Skyrim.esm:0x06CB48 VendorItemsMisc)"
            },
            "notSellBuyList": {
              "type": "boolean",
              "description": "true => sellBuyList is a NOT-sell list (trade everything except)"
            },
            "merchantContainer": {
              "$ref": "#/$defs/ref",
              "description": "a PLACEMENT editorId: the placed merchant chest (gold + stock)"
            }
          }
        }
      }
    },
    "book": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "text": {
          "type": "string"
        },
        "template": {
          "type": "string",
          "description": "ref to a vanilla BOOK to clone a model from; a takeable/readable book needs one or it CRASHES on read"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "flags": {
          "type": "array",
          "items": {
            "enum": [
              "Teaches",
              "CantBeTaken"
            ]
          },
          "description": "Book.Flag names"
        },
        "teaches": {
          "type": "object",
          "description": "a teaching book: a spell tome (grants a SPEL) or a skill book (raises a Skill) on first read",
          "properties": {
            "kind": {
              "enum": [
                "spell",
                "skill"
              ]
            },
            "spell": {
              "type": "string",
              "description": "ref to a SPEL (in-spec editorId or <master>:0xFORMID); when kind=spell"
            },
            "skill": {
              "enum": [
                "OneHanded",
                "TwoHanded",
                "Archery",
                "Block",
                "Smithing",
                "HeavyArmor",
                "LightArmor",
                "Pickpocket",
                "Lockpicking",
                "Sneak",
                "Alchemy",
                "Speech",
                "Alteration",
                "Conjuration",
                "Destruction",
                "Illusion",
                "Restoration",
                "Enchanting"
              ],
              "description": "when kind=skill"
            }
          }
        }
      }
    },
    "armor": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "armorRating": {
          "type": "number",
          "minimum": 0
        },
        "armorType": {
          "enum": [
            "light",
            "heavy",
            "clothing",
            "lightArmor",
            "heavyArmor"
          ],
          "description": "armor class; default clothing"
        },
        "slots": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "biped slots occupied, e.g. Body, Head, Hands, Feet, Forearms, Calves, Shield (BipedObjectFlag names)"
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        },
        "enchantment": {
          "$ref": "#/$defs/ref",
          "description": "ENCH ref — normally an in-spec apparel (constant-effect) enchantments entry, or a vanilla ObjectEffect (<master>:0xFORMID)"
        },
        "template": {
          "type": "string",
          "description": "vanilla armor to clone for its Armature (worn mesh) + WorldModel + BodyTemplate (<master>:0xFORMID, e.g. Skyrim.esm:0x00012E49 ArmorIronCuirass). REQUIRED for the armor to be visible when equipped — a bare ARMO equips INVISIBLE."
        },
        "model": {
          "type": "string",
          "description": "optional ground/world model .nif path; overrides the cloned template's. The worn mesh still comes from the template's Armature, so pair `model` with a `template`."
        }
      }
    },
    "message": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        }
      }
    },
    "class": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "Class (CLAS): an npc 'profession' — attribute/skill weights",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "teaches": {
          "enum": [
            "OneHanded",
            "TwoHanded",
            "Archery",
            "Block",
            "Smithing",
            "HeavyArmor",
            "LightArmor",
            "Pickpocket",
            "Lockpicking",
            "Sneak",
            "Alchemy",
            "Speech",
            "Alteration",
            "Conjuration",
            "Destruction",
            "Illusion",
            "Restoration",
            "Enchanting"
          ],
          "description": "a Skill the class can train (trainer NPCs)"
        },
        "maxTrainingLevel": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "healthWeight": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "magickaWeight": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "staminaWeight": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "skillWeights": {
          "type": "object",
          "additionalProperties": {
            "type": "integer",
            "minimum": 0,
            "maximum": 255
          },
          "description": "Skill name -> 0-255 favour, e.g. {\"Destruction\":100}"
        }
      }
    },
    "npc": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "factions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          },
          "description": "faction refs (in-spec or external)"
        },
        "race": {
          "$ref": "#/$defs/ref",
          "description": "Race ref; needed (with class) for a functional actor"
        },
        "class": {
          "$ref": "#/$defs/ref",
          "description": "Class ref"
        },
        "outfit": {
          "$ref": "#/$defs/ref",
          "description": "Outfit ref -> DefaultOutfit"
        },
        "level": {
          "type": "integer",
          "minimum": 0,
          "description": "fixed level (0 = default); needed for class-based stat auto-calc"
        },
        "autoCalcStats": {
          "type": "boolean",
          "description": "derive H/M/S + skills from level + class (else flat 50/50/50 defaults)"
        },
        "packages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          },
          "description": "PACK refs; the NPC's AI package list, evaluated in order"
        },
        "voiceType": {
          "$ref": "#/$defs/ref",
          "description": "VTYP ref (e.g. MaleNord Skyrim.esm:0x013AE6); without one the NPC is silent"
        },
        "voiceTemplate": {
          "type": "string",
          "description": "ref → voiceTemplates ID; routes this NPC's lines to a cloning engine"
        },
        "crimeFaction": {
          "$ref": "#/$defs/ref",
          "description": "FACT ref; city-citizen identity, required for cross-cell Travel"
        },
        "unique": {
          "type": "boolean",
          "description": "one-off actor (vs leveled); helps engine AI tracking + cross-cell travel"
        },
        "combatStyle": {
          "$ref": "#/$defs/ref",
          "description": "CSTY ref; HOW the AI fights (magic/melee/ranged preference)"
        },
        "spells": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          },
          "description": "SPEL refs; the AI's spell list"
        },
        "perks": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          },
          "description": "PERK refs granted to the actor as passive ability/entry-point perks at game start"
        },
        "aggression": {
          "enum": [
            "Unaggressive",
            "Aggressive",
            "VeryAggressive",
            "Frenzied"
          ],
          "description": "WHETHER it initiates (default Unaggressive)"
        },
        "confidence": {
          "enum": [
            "Cowardly",
            "Cautious",
            "Average",
            "Brave",
            "Foolhardy"
          ],
          "description": "flee-vs-stand once attacked (default Cowardly = flees)"
        },
        "assistance": {
          "enum": [
            "HelpsNobody",
            "HelpsAllies",
            "HelpsFriendsAndAllies"
          ]
        },
        "mood": {
          "enum": [
            "Neutral",
            "Angry",
            "Fear",
            "Happy",
            "Sad",
            "Surprised",
            "Puzzled",
            "Disgusted"
          ]
        },
        "energyLevel": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "vanilla actors typically 50"
        },
        "greeting": {
          "type": "string",
          "description": "Hello line; when this NPC has custom dialogue, a Hello info is auto-emitted so it is conversable. Empty => default line"
        },
        "essential": {
          "type": "boolean",
          "description": "cannot be killed (drops to bleedout then recovers) — for a non-lethal brawl or plot-critical NPC. NOTE: an essential NPC also CANNOT be looted; use `protected` instead if the player should be able to loot custom gear"
        },
        "protected": {
          "type": "boolean",
          "description": "can only be killed by the PLAYER (other NPCs can't land the killing blow); lootable, unlike essential"
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/npcItem"
          },
          "description": "inventory the NPC carries — a weapon/armor here is auto-equipped if it's the NPC's best (how you arm an NPC); loot drops on death. A 0-damage weapon scores below fists and won't be drawn"
        }
      }
    },
    "relationship": {
      "type": "object",
      "required": [
        "editorId",
        "parent"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "parent": {
          "$ref": "#/$defs/ref",
          "description": "NPC_ base ref (the relationship owner; usually the custom NPC)"
        },
        "child": {
          "$ref": "#/$defs/ref",
          "description": "NPC_ base ref; defaults to the Player NPC base Skyrim.esm:0x000014. NOTE: must be an NPC_, NOT PlayerRef 0x000007 (an ACHR) — that CRASHES on load"
        },
        "rank": {
          "enum": [
            "Lover",
            "Ally",
            "Confidant",
            "Friend",
            "Acquaintance",
            "Rival",
            "Foe",
            "Enemy",
            "Archnemesis"
          ],
          "description": "Ally satisfies the vanilla free-follow gate"
        }
      }
    },
    "combatStyle": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "offensiveMult": {
          "type": "number"
        },
        "defensiveMult": {
          "type": "number"
        },
        "groupOffensiveMult": {
          "type": "number"
        },
        "equipMultMelee": {
          "type": "number"
        },
        "equipMultMagic": {
          "type": "number",
          "description": "raise well above the others to make the AI prefer spells"
        },
        "equipMultRanged": {
          "type": "number"
        },
        "equipMultShout": {
          "type": "number"
        },
        "equipMultUnarmed": {
          "type": "number"
        },
        "equipMultStaff": {
          "type": "number"
        },
        "avoidThreatChance": {
          "type": "number"
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "CombatStyle.Flag names, e.g. Dueling"
        }
      }
    },
    "encounterZone": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "EncounterZone (ECZN): level scaling + respawn for an area; a cell/spawn `encounterZone` points at one",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "minLevel": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255,
          "description": "floor of the spawn level range"
        },
        "maxLevel": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255,
          "description": "cap of the spawn level range; 0 = UNCAPPED (scales with the player — the vanilla dungeon idiom)"
        },
        "rank": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255,
          "description": "owner rank (0 if no owner)"
        },
        "owner": {
          "$ref": "#/$defs/ref",
          "description": "optional FACT/NPC owner ref"
        },
        "location": {
          "$ref": "#/$defs/ref",
          "description": "optional LCTN ref (map location)"
        },
        "flags": {
          "type": "array",
          "items": {
            "enum": [
              "NeverResets",
              "MatchPcBelowMinimumLevel",
              "DisableCombatBoundary"
            ]
          },
          "description": "NeverResets = no respawn; MatchPcBelowMinimumLevel = below-min players get player-level spawns; DisableCombatBoundary = actors may chase out of the zone"
        }
      }
    },
    "package": {
      "type": "object",
      "required": [
        "editorId",
        "template"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "template": {
          "$ref": "#/$defs/ref",
          "description": "vanilla procedure template, e.g. Sandbox=Skyrim.esm:0x01C254, Travel=Skyrim.esm:0x016FAA"
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Package.Flag names"
        },
        "interruptFlags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "HellosToPlayer / AllowIdleChatter / WorldInteractions / ..."
        },
        "preferredSpeed": {
          "enum": [
            "Walk",
            "Jog",
            "Run",
            "FastWalk"
          ]
        },
        "combatStyle": {
          "$ref": "#/$defs/ref"
        },
        "ownerQuest": {
          "$ref": "#/$defs/ref",
          "description": "Quest that owns this radiant package. Must be an in-spec quest if any target/location slot uses an 'alias:<name>'/'aliasLoc:<name>' ref (the alias index resolves against this quest's aliases). See SPEC-packages.md."
        },
        "schedule": {
          "type": "object",
          "description": "{ month, dayOfWeek, date, hour, minute, durationInMinutes }"
        },
        "sandbox": {
          "type": "object",
          "description": "Sandbox template inputs (radius, allowEating, ...); see SPEC.md"
        },
        "travel": {
          "type": "object",
          "description": "Travel template inputs (destination, ...); see SPEC.md"
        },
        "useMagic": {
          "type": "object",
          "description": "UseMagic template inputs (spell, numToCastMax, ...); see SPEC.md"
        },
        "sitTarget": {
          "type": "object",
          "description": "SitTarget template (Skyrim.esm:0x0A9277) inputs: target (REQUIRED furniture ref), waitTime, stopMovement — NPC walks to and sits on the furniture; see SPEC-packages.md"
        },
        "activate": {
          "type": "object",
          "description": "Activate template inputs (target [required], numberToActivate); see SPEC.md"
        },
        "eat": {
          "type": "object",
          "description": "Eat template inputs (location, radius, allowSitting, ...); see SPEC.md"
        }
      }
    },
    "quest": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "startGameEnabled": {
          "type": "boolean",
          "description": "auto-start on load (default true; required for hosted dialogue)"
        },
        "priority": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "type": {
          "enum": [
            "None",
            "MainQuest",
            "MageGuild",
            "ThievesGuild",
            "DarkBrotherhood",
            "CompanionQuests",
            "Misc",
            "Daedric",
            "SideQuest",
            "CivilWar",
            "Vampire",
            "Dragonborn"
          ],
          "description": "Quest TYPE (DNAM) — which journal tab it groups under. type=None = a background/controller quest that does NOT appear in the journal (its stage log entries never show). Leave empty for the smart default: a quest with journal content -> SideQuest; a pure dialogue/controller quest -> None"
        },
        "objectives": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "index"
            ],
            "properties": {
              "index": {
                "type": "integer",
                "minimum": 0
              },
              "text": {
                "type": "string"
              },
              "showStage": {
                "type": "integer",
                "description": "stage index that displays this objective (-1 = none)"
              },
              "completeStage": {
                "type": "integer",
                "description": "stage index that completes this objective (-1 = none)"
              },
              "targets": {
                "type": "array",
                "description": "QSTA compass/map markers; the arrow follows the named alias's fill",
                "items": {
                  "type": "object",
                  "required": [
                    "alias"
                  ],
                  "properties": {
                    "alias": {
                      "type": "string",
                      "description": "alias NAME on the same quest"
                    },
                    "compassIgnoresLocks": {
                      "type": "boolean"
                    },
                    "conditions": {
                      "type": "array",
                      "items": {
                        "$ref": "#/$defs/condition"
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "stages": {
          "type": "array",
          "description": "quest milestones; index unique + ascending. See SPEC.md 'quest stages'.",
          "items": {
            "type": "object",
            "required": [
              "index"
            ],
            "properties": {
              "index": {
                "type": "integer",
                "minimum": 0
              },
              "logEntry": {
                "type": "string",
                "description": "journal text shown when the quest reaches this stage"
              },
              "completeQuest": {
                "type": "boolean",
                "description": "close the quest at this stage"
              },
              "failQuest": {
                "type": "boolean",
                "description": "fail the quest at this stage"
              },
              "startUpStage": {
                "type": "boolean",
                "description": "engine auto-runs SetStage to this stage on quest start (QSDT Start Up Stage flag) — how an SM-triggered quest shows its opening log entry/objective with no external SetStage. At most one per quest."
              },
              "conditions": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/condition"
                },
                "description": "optional CTDA gate on the log entry (e.g. function GetStage, param the quest ref)"
              },
              "instanceGlobals": {
                "type": "array",
                "description": "Globals bound to THIS quest instance when the stage runs (gather/count radiant quests): the stage fragment calls UpdateCurrentInstanceGlobal(<global>) so objective text '<Global=X>' reads per-instance. Optionally seeds each global first (random range / fixed value).",
                "items": {
                  "type": "object",
                  "required": [ "global" ],
                  "properties": {
                    "global": { "type": "string", "description": "GLOB editorId (declare in spec.globals) or a vanilla ref" },
                    "randomMin": { "type": "integer", "description": "with randomMax → SetValue(Utility.RandomInt(min,max)) before binding" },
                    "randomMax": { "type": "integer" },
                    "value": { "type": "number", "description": "else if set → SetValue(value) before binding; omit both to bind without changing the value" }
                  },
                  "additionalProperties": false
                }
              },
              "globalWrites": {
                "type": "array",
                "description": "First-class GlobalVariable writes when the quest reaches this stage (K group): the stage fragment emits <global>.SetValue(value). A plain write (no UpdateCurrentInstanceGlobal). For an SM-driven quest it runs in the OnStory<Event> handler.",
                "items": {
                  "type": "object",
                  "required": [ "global", "value" ],
                  "additionalProperties": false,
                  "properties": {
                    "global": { "type": "string", "description": "GLOB editorId (declare in spec.globals) or a vanilla ref" },
                    "value": { "type": "number", "description": "the value to SetValue" }
                  }
                }
              },
              "persist": { "$ref": "#/$defs/persist", "description": "JContainers JFormDB writes performed when the quest REACHES this stage (Idea #20 Phase 0). key must be 'player' or a ref — a stage has no speaker." },
              "syncPerks": { "$ref": "#/$defs/syncPerks", "description": "AddPerk/RemovePerk from stored ranks when the quest reaches this stage. key must be 'player' or a ref." }
            }
          }
        },
        "storyEvent": {
          "type": "object",
          "description": "Story Manager trigger: this quest starts when the named event fires. Build auto-sets startGameEnabled=false and wires the SM tree.",
          "properties": {
            "event": {
              "type": "string",
              "description": "Event name: KillActor | ChangeLocation | CastMagic | AddItem | Assault | CraftItem | PlayerRemoveItem | Arrest | IncreaseLevel | ScriptEvent"
            },
            "conditions": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/condition"
              },
              "description": "CTDA gates on the SM branch (e.g. GetIsID victim)"
            },
            "keyword": {
              "type": "string",
              "description": "ScriptEvent only: editorId of a KYWD declared in spec.keywords; the dispatcher passes it to SendStoryEvent and the branch filters on it"
            },
            "locationFilter": {
              "type": "array",
              "items": { "type": "string" },
              "description": "#5 location-aware encounter: LocType keyword refs. Build appends OR'd GetKeywordDataForCurrentLocation event-conditions (fires only when the new location has ANY listed LocType). Best with event=ChangeLocation."
            },
            "cooldownHours": {
              "type": "number",
              "minimum": 0,
              "description": "#6 anti-spam cooldown (EE_WITimeout): min in-game hours between firings. Creates a <quest>_LastFired GLOB + attaches the reusable MFEncounterCooldown quest script. 0 = none."
            }
          }
        },
        "spawn": {
          "type": "object",
          "required": [ "form" ],
          "description": "#3 dynamic near-player spawn: on quest start, place `count` of `form` (ActorBase/LeveledNpc) at a random minDistance..maxDistance offset around the player + snap to navmesh (the EE NavmeshTester trick). Attaches the reusable MFDynamicSpawn quest script.",
          "properties": {
            "form": { "$ref": "#/$defs/ref", "description": "ActorBase (NPC_) or LeveledNpc (LVLN) to spawn" },
            "count": { "type": "integer", "minimum": 1, "default": 1 },
            "minDistance": { "type": "number", "minimum": 0, "default": 1500 },
            "maxDistance": { "type": "number", "minimum": 0, "default": 4000 },
            "snapToNavmesh": { "type": "boolean", "default": true, "description": "EnableAI toggle to snap each spawn to the nearest navmesh point" }
          },
          "additionalProperties": false
        },
        "aliases": {
          "type": "array",
          "description": "Quest aliases filled by the SM event or forced refs.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "alias name (must be unique within the quest)"
              },
              "fill": {
                "type": "string",
                "description": "fromEvent:<slot> (R1/R2/L1/L2) | forced:<ref> | uniqueActor:<ref> | createObject:<ref>@<targetAlias> (spawn a new ref AT another ref-type alias) | findMatching:closest|any (nearest/first existing ref in the loaded area matching this alias's conditions) | findMatchingLocation:<locType>[@<parentAlias>] (Location-type alias: pick a location of this LocType keyword, optionally within a parent location alias) | findInLocationAlias:<locationAlias>[#<refTypeLCRT>] (Reference-type alias: find a ref of this LocationRefType within a location alias)"
              },
              "optional": {
                "type": "boolean",
                "description": "quest still starts even if this alias cannot be filled"
              },
              "allowReserved": {
                "type": "boolean",
                "description": "allow filling with a ref held by another running quest (uniqueActor forces this on)"
              },
              "conditions": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/condition"
                },
                "description": "findMatching only (required): CTDA filter on WHICH loaded-area ref the engine picks (e.g. HasKeyword ActorTypeNPC = nearest NPC)"
              },
              "script": {
                "type": "string",
                "description": "optional alias script Scriptname (extends ReferenceAlias) attached to this alias's VMAD — reacts to events on whatever ref fills it (e.g. OnActivate on a createObject-spawned ref to chain a story event via MFStoryEventDispatch.Fire)"
              },
              "scriptSource": {
                "type": "string",
                "description": "the alias script's .psc, compiled by `package` (you supply/ship the .pex)"
              },
              "scriptProperties": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/property"
                },
                "description": "typed Auto properties bound on the alias-script VMAD (e.g. a Keyword for a dispatcher trigger)"
              }
            }
          }
        }
      }
    },
    "dialogue": {
      "type": "object",
      "required": [
        "editorId",
        "questEditorId",
        "prompt"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "questEditorId": {
          "type": "string",
          "minLength": 1,
          "description": "must be a quest editorId in this spec"
        },
        "speakerNpcEditorId": {
          "type": "string",
          "description": "optional; an npc editorId (GetIsID condition). The speaker also needs a `greeting` so it is conversable"
        },
        "prompt": {
          "type": "string",
          "minLength": 1,
          "description": "the player's topic line"
        },
        "responses": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "the NPC's spoken lines"
        },
        "emotion": {
          "enum": [
            "Neutral",
            "Anger",
            "Disgust",
            "Fear",
            "Sad",
            "Happy",
            "Surprise",
            "Puzzled"
          ],
          "description": "applied to all response lines (default Neutral)"
        },
        "emotionValue": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "emotion intensity (default 50)"
        },
        "setStage": {
          "type": "integer",
          "description": "advance the host quest to this stage when the line is picked (-1 = none); needs the generated TIF fragment compiled in CK"
        },
        "openBarter": {
          "type": "boolean",
          "description": "open the BARTER/trade menu with the speaking NPC when picked (vanilla Actor.ShowBarterMenu(), no SKSE); speaker must be a vendor-faction member with a merchant chest; pair with an identity gate for a merchant-only trade option"
        },
        "setGlobal": {
          "type": "object",
          "description": "write or adjust one GlobalVariable when the line is picked. Use value for flags/absolute reputation states (SetValue) or delta for counters (Mod). The GLOB can then gate dialogue/identity.activeWhen via GetGlobalValue.",
          "required": [
            "global"
          ],
          "properties": {
            "global": {
              "$ref": "#/$defs/ref",
              "description": "the GLOB to modify"
            },
            "value": {
              "type": "number",
              "description": "absolute value to write with SetValue"
            },
            "delta": {
              "type": "number",
              "description": "relative counter change to apply with GlobalVariable.Mod; positive increments, negative decrements"
            }
          },
          "oneOf": [
            {
              "required": [
                "value"
              ]
            },
            {
              "required": [
                "delta"
              ]
            }
          ]
        },
        "rewardItem": {
          "$ref": "#/$defs/ref",
          "description": "give the player this item/gold when the line is picked (Game.GetPlayer().AddItem); use with setStage for a quest reward"
        },
        "rewardCount": {
          "type": "integer",
          "default": 1,
          "description": "how many of rewardItem to give (default 1)"
        },
        "evaluateSpeakerPackages": {
          "type": "boolean",
          "description": "force the speaking NPC to re-evaluate AI packages (Actor.EvaluatePackage()) so a package newly enabled by this line's setStage (e.g. a follow package gated on GetStage==N) activates immediately"
        },
        "persist": { "$ref": "#/$defs/persist" },
        "syncPerks": { "$ref": "#/$defs/syncPerks" },
        "resultScript": {
          "type": "string",
          "description": "custom result-script Scriptname (extends TopicInfo) wired as the line's OnBegin Fragment_0; overrides the auto setStage TIF"
        },
        "resultScriptSource": {
          "type": "string",
          "description": "optional .psc path (rel. to spec) for resultScript; `package` compiles it (beside the embedded dispatcher header for Fire()-routed triggers)"
        },
        "resultProperties": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/property"
          },
          "description": "typed properties bound on the result-script VMAD (e.g. a Keyword for a dispatcher trigger)"
        },
        "goodbye": {
          "type": "boolean",
          "description": "close the dialogue menu after this line (vanilla recruit/dismiss lines set it)"
        },
        "sayOnce": {
          "type": "boolean",
          "description": "INFO (ENAM) flag: speak this line at most once ever (VIGILANT's most-used flag — one-shot story beats)"
        },
        "walkAway": {
          "type": "boolean",
          "description": "INFO (ENAM) flag: the NPC walks off after delivering this line"
        },
        "random": {
          "type": "boolean",
          "description": "INFO (ENAM) flag: the engine random-picks among sibling INFOs whose conditions pass (line variety)"
        },
        "invisibleContinue": {
          "type": "boolean",
          "description": "INFO (ENAM) flag: immediately continue to the next INFO in the chain without closing the menu"
        },
        "forceSubtitle": {
          "type": "boolean",
          "description": "INFO (ENAM) flag: always show the subtitle even when subtitles are off"
        },
        "topLevel": {
          "type": "boolean",
          "description": "dialogue tree: false = a SUB-topic that only appears when another line linkTo's it (default true = a normal top-level option)"
        },
        "linkTo": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "dialogue tree (ENAM): after this line, surface these dialogue topics as the next choices — each is another dialogue's editorId or a vanilla <master>:0xFORMID topic"
        },
        "previousDialog": {
          "type": "string",
          "description": "dialogue tree (PNAM): chain this INFO after another (a dialogue editorId or <master>:0xFORMID INFO)"
        },
        "hello": {
          "type": "boolean",
          "description": "emit this line as the NPC's auto-spoken GREETING (Misc/Hello) instead of a player menu option; combine with identity/primaryIdentity/conditions to greet differently by state (engine picks the highest-priority matching Hello, else the NPC's plain greeting). prompt is ignored."
        },
        "identity": {
          "type": "string",
          "description": "gate this line on the player HOLDING this identity (identities[].id) — expands to GetInFaction>=1 (+ the identity's activeWhen)"
        },
        "primaryIdentity": {
          "type": "string",
          "description": "gate this line on this identity being the player's CURRENT PRIMARY (the controller-maintained MF_PrimaryIdentity global == its code) — used for state-varying greetings"
        },
        "setPrimaryIdentity": {
          "type": "string",
          "description": "when the player picks this topic, manually OVERRIDE the primary identity to this id (or 'auto' to clear) — generates a TIF fragment setting MF_IdentityOverride; pair with an identity gate"
        },
        "conditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/condition"
          },
          "description": "extra CTDA gates on the INFO beyond the auto GetIsID speaker gate (e.g. only show a paid recruit line when the player can afford it)"
        }
      }
    },
    "script": {
      "type": "object",
      "required": [
        "targetEditorId",
        "scriptName"
      ],
      "properties": {
        "targetEditorId": {
          "type": "string",
          "minLength": 1,
          "description": "record to attach to (any editorId)"
        },
        "scriptName": {
          "type": "string",
          "minLength": 1,
          "description": "must match the .pex/.psc Scriptname"
        },
        "source": {
          "type": "string",
          "description": "optional .psc path (rel. to spec); `package` compiles it"
        },
        "properties": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/property"
          }
        }
      }
    },
    "property": {
      "type": "object",
      "required": [
        "name",
        "type"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "type": {
          "enum": [
            "int",
            "float",
            "bool",
            "string",
            "object"
          ]
        },
        "int": {
          "type": "integer"
        },
        "float": {
          "type": "number"
        },
        "bool": {
          "type": "boolean"
        },
        "str": {
          "type": "string"
        },
        "objectEditorId": {
          "type": "string",
          "description": "required when type=object; an editorId"
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "object"
              }
            }
          },
          "then": {
            "required": [
              "objectEditorId"
            ]
          }
        }
      ]
    },
    "cell": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "description": "display name"
        },
        "template": {
          "type": "string",
          "description": "optional vanilla INTERIOR cell '<master>:0xFORMID' to copy lighting/water env from (a new cell with no lighting renders BLACK; find one with `find <Skyrim.esm> <name> Cell`). Still place a floor static via a `placement`, else the player falls into the void."
        },
        "encounterZone": {
          "$ref": "#/$defs/ref",
          "description": "optional ECZN ref (in-spec editorId or '<master>:0xFORMID') — the whole cell's level scaling / respawn (XEZN)"
        },
        "lightingTemplate": {
          "type": "string",
          "description": "optional LGTM ref: an in-spec lightingTemplates entry editorId OR a vanilla '<master>:0xFORMID' (e.g. Skyrim.esm:0x0300E2 = DefaultLightingTemplate). Controls ambient/directional/fog/DALC for the cell."
        },
        "imageSpace": {
          "type": "string",
          "description": "optional IMGS ref: an in-spec imageSpaces entry editorId OR a vanilla '<master>:0xFORMID'. Controls HDR eye-adapt / bloom / cinematic look for the cell."
        },
        "music": {
          "type": "string",
          "description": "optional MUSC ref: an in-spec music[] entry editorId OR a vanilla '<master>:0xFORMID'. The music type that plays in this cell."
        },
        "lighting": {
          "$ref": "#/$defs/cellLighting",
          "description": "optional inline XCLL overrides; fields left null are pulled from the lightingTemplate. Use when you need per-cell tweaks on top of the shared template."
        }
      }
    },
    "vec3": {
      "type": "object",
      "properties": {
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        },
        "z": {
          "type": "number"
        }
      }
    },
    "placement": {
      "type": "object",
      "required": [
        "base"
      ],
      "description": "set EITHER `cell` (interior) OR `worldspace` (exterior); worldspace wins if both are set",
      "oneOf": [
        {
          "required": [
            "cell"
          ]
        },
        {
          "required": [
            "worldspace"
          ]
        }
      ],
      "properties": {
        "base": {
          "$ref": "#/$defs/ref",
          "description": "the form to place: an in-spec npc/object editorId or external <master>:0xFORMID. May also be a LeveledNpc (LVLN) list for a LEVELED-ACTOR SPAWN — the ACHR then rolls a level-appropriate actor (add kind:\"npc\" for a vanilla list). MARKERS: XMarker = Skyrim.esm:0x00003B, XMarkerHeading = Skyrim.esm:0x000034 (a Travel/Patrol/scene anchor). DOORS: a load-door base (e.g. FarmhouseLDoor01 = Skyrim.esm:0x029CB0) + `teleport` makes a walk-through link"
        },
        "editorId": {
          "type": "string",
          "description": "optional: names this REFR/ACHR so other refs can target it (patrol start, linkedRefs target, teleport partner). Must be unique"
        },
        "cell": {
          "type": "string",
          "minLength": 1,
          "description": "INTERIOR target: an in-spec cell editorId OR a vanilla INTERIOR cell '<master>:0xFORMID' (find with `find <Skyrim.esm> <name> Cell`)"
        },
        "worldspace": {
          "type": "string",
          "minLength": 1,
          "description": "EXTERIOR target: a worldspace '<master>:0xFORMID' (Tamriel = Skyrim.esm:0x00003C; find with `find <Skyrim.esm> <name> Worldspace`). `position` is then WORLD coords; the cell at floor(x/4096),floor(y/4096) is overridden"
        },
        "kind": {
          "enum": [
            "npc",
            "object",
            "hazard",
            "xmarker",
            "xmarkerHeading"
          ],
          "description": "inferred for in-spec bases; defaults to object for external. xmarker/xmarkerHeading default an empty base to the vanilla marker static + force persistent"
        },
        "position": {
          "$ref": "#/$defs/vec3",
          "description": "cell-local for interior; WORLD coords for exterior"
        },
        "rotation": {
          "$ref": "#/$defs/vec3",
          "description": "degrees"
        },
        "persistent": {
          "type": "boolean",
          "description": "place in the cell's persistent (vs temporary) list (teleport doors / linkedRefs sources / package anchors are forced persistent automatically)"
        },
        "encounterZone": {
          "$ref": "#/$defs/ref",
          "description": "optional ECZN ref — a per-ref override of the cell's zone (this spawn's XEZN); usually only meaningful on a leveled-actor spawn"
        },
        "teleport": {
          "type": "string",
          "description": "LOAD-DOOR pair: the PARTNER door this door teleports to (a placement editorId, or a vanilla door '<master>:0xFORMID'). Set on BOTH doors (each pointing at the other). `base` must be a DOOR. Arrival = the partner's position/rotation (written to this door's XTEL)"
        },
        "linkedRefs": {
          "type": "array",
          "description": "Linked References (XLKR) on this ref — the Patrol route chain etc. Source placement must have an editorId",
          "items": {
            "type": "object",
            "required": [
              "target"
            ],
            "properties": {
              "target": {
                "$ref": "#/$defs/ref",
                "description": "the linked placed ref (a placement editorId or external ref)"
              },
              "keyword": {
                "$ref": "#/$defs/ref",
                "description": "optional KYWD tagging the link; empty = the null/default link a Patrol route follows"
              }
            }
          }
        },
        "scale": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "XSCL: uniform scale multiplier (1.0 = default, XSCL subrecord omitted). Valid for statics/furniture/lights; actors ignore it in-game"
        },
        "initiallyDisabled": {
          "type": "boolean",
          "description": "Record flag 0x800: the ref exists in the cell but is invisible/non-collidable until explicitly enabled. Pair with enableParent to control when it appears"
        },
        "enableParent": {
          "type": "object",
          "description": "XESP: this ref's enabled state follows another placed ref (the 'enable parent')",
          "required": ["ref"],
          "properties": {
            "ref": {
              "$ref": "#/$defs/ref",
              "description": "the controlling placed ref: a placement editorId or <master>:0xFORMID"
            },
            "flag": {
              "enum": ["SetEnable", "SetDisable", "PopIn"],
              "description": "SetEnable (default): I enable when my parent enables. SetDisable: I disable when my parent enables (inverted). PopIn: no fade-in animation when I appear (avoid the flash)"
            }
          }
        },
        "lock": {
          "type": "object",
          "description": "XLOC: lock a door or container at a given difficulty (PlacedObject only)",
          "required": ["level"],
          "properties": {
            "level": {
              "type": "string",
              "description": "difficulty: novice | apprentice | adept | expert | master | requiresKey | inaccessible, or a raw byte value as a string (\"50\")"
            },
            "key": {
              "$ref": "#/$defs/ref",
              "description": "optional item ref (KEYM or any item) that bypasses the lock"
            }
          }
        },
        "ownership": {
          "type": "object",
          "description": "XOWN: who owns this placed object — picking it up counts as theft if the owner is alive/active",
          "required": ["owner"],
          "properties": {
            "owner": {
              "$ref": "#/$defs/ref",
              "description": "FACT or NPC ref: the faction or individual who owns this object"
            },
            "rank": {
              "type": "integer",
              "minimum": 0,
              "description": "faction rank required for ownership (0 = any member; ignored when owner is an NPC)"
            }
          }
        },
        "count": {
          "type": "integer",
          "minimum": 0,
          "description": "XCNT: item stack count for a placed item (e.g. 50 gold coins on a table). 0 = omitted (single instance). Not meaningful for actors or statics"
        }
      }
    },
    "leveledEntry": {
      "type": "object",
      "required": [
        "reference"
      ],
      "properties": {
        "reference": {
          "$ref": "#/$defs/ref",
          "description": "the item (LVLI) or npc (LVLN) to spawn — in-spec or external"
        },
        "level": {
          "type": "integer",
          "minimum": 1,
          "description": "min player/encounter level for this entry (default 1)"
        },
        "count": {
          "type": "integer",
          "minimum": 1,
          "description": "how many (default 1)"
        }
      }
    },
    "leveledList": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "chanceNone": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "percent chance the list yields nothing"
        },
        "flags": {
          "type": "array",
          "items": {
            "enum": [
              "CalculateFromAllLevelsLessThanOrEqualPlayer",
              "CalculateForEachItemInCount",
              "UseAll",
              "SpecialLoot"
            ]
          }
        },
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/leveledEntry"
          }
        }
      }
    },
    "containerItem": {
      "type": "object",
      "required": [
        "item"
      ],
      "properties": {
        "item": {
          "$ref": "#/$defs/ref",
          "description": "the item to put inside — in-spec or external"
        },
        "count": {
          "type": "integer",
          "minimum": 1,
          "description": "default 1"
        }
      }
    },
    "container": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/containerItem"
          }
        }
      }
    },
    "recipe": {
      "type": "object",
      "required": [
        "editorId",
        "createdObject"
      ],
      "description": "ConstructibleObject (COBJ): a crafting recipe",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "kind": {
          "enum": [
            "craft",
            "temper",
            "smelt",
            "breakdown"
          ],
          "default": "craft",
          "description": "recipe flavour; picks the default bench (craft->forge, temper->sharpening wheel, smelt/breakdown->smelter)"
        },
        "createdObject": {
          "$ref": "#/$defs/ref",
          "description": "the item produced (temper: the item being improved) — usually an in-spec weapon/armor/etc."
        },
        "count": {
          "type": "integer",
          "minimum": 1,
          "description": "how many are produced (default 1)"
        },
        "workbench": {
          "type": "string",
          "description": "named selector (forge|sharpeningWheel|armorTable|smelter|tanningRack|skyforge) OR a keyword ref; omit -> the kind default"
        },
        "components": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/containerItem"
          },
          "description": "ingredients consumed on craft (item ref + count)"
        },
        "conditions": {
          "type": "array",
          "description": "perk/item/skill gating (shared CTDA ConditionSpec)",
          "items": {
            "type": "object",
            "properties": {
              "function": {
                "enum": [
                  "HasPerk",
                  "GetItemCount",
                  "GetGlobalValue",
                  "TemperIsEnchanted"
                ],
                "description": "the CTDA function; HasPerk/GetItemCount/GetGlobalValue need a param, TemperIsEnchanted does not"
              },
              "param": {
                "type": "string",
                "description": "the function's form arg (perk/item/global ref)"
              },
              "comparison": {
                "enum": [
                  "==",
                  "=",
                  "!=",
                  ">",
                  ">=",
                  "<",
                  "<="
                ],
                "description": "the comparison operator (default >=)"
              },
              "value": {
                "type": "number",
                "description": "the test value (default 0)"
              },
              "or": {
                "type": "boolean",
                "description": "OR-chain with the NEXT condition (default AND)"
              }
            }
          }
        }
      }
    },
    "ingredient": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "effects": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/effect"
          }
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        }
      }
    },
    "ammunition": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "damage": {
          "type": "number",
          "minimum": 0
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        }
      }
    },
    "scroll": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "effects": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/effect"
          }
        },
        "spellType": {
          "enum": [
            "Spell",
            "Disease",
            "Power",
            "LesserPower",
            "Ability",
            "Poison",
            "Addiction",
            "Voice"
          ]
        },
        "castType": {
          "enum": [
            "ConstantEffect",
            "FireAndForget",
            "Concentration"
          ]
        },
        "targetType": {
          "enum": [
            "Self",
            "Touch",
            "Aimed",
            "TargetActor",
            "TargetLocation"
          ]
        },
        "baseCost": {
          "type": "integer",
          "minimum": 0
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        }
      }
    },
    "soulGem": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "maximumCapacity": {
          "enum": [
            "None",
            "Petty",
            "Lesser",
            "Common",
            "Greater",
            "Grand"
          ]
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        }
      }
    },
    "key": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "integer",
          "minimum": 0
        },
        "weight": {
          "type": "number",
          "minimum": 0
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        }
      }
    },
    "keyword": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1,
          "description": "reference it from any record's `keywords` list"
        }
      }
    },
    "outfit": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          },
          "description": "armor/weapon refs the NPC wears; an npc `outfit` can point at this editorId"
        }
      }
    },
    "staticRec": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "model": {
          "type": "string",
          "description": "a .nif path, Data-relative rooted at Meshes\\ (omit the 'Meshes\\' prefix), vanilla OR custom, e.g. 'Clutter\\\\Common\\\\Rock01.nif' or 'MyMod\\\\stone.nif'"
        },
        "alternateTextures": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/alternateTexture"
          },
          "description": "retexture named materials of `model` via TXST records"
        }
      }
    },
    "activator": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "model": {
          "type": "string",
          "description": "a .nif path (rooted at Meshes\\, omit the prefix)"
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        },
        "alternateTextures": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/alternateTexture"
          },
          "description": "retexture named materials of `model` via TXST records"
        },
        "activationSound": {
          "$ref": "#/$defs/ref",
          "description": "SNDR ref (in-spec sounds editorId or vanilla <master>:0xFORMID) played on activation"
        },
        "loopingSound": {
          "$ref": "#/$defs/ref",
          "description": "SNDR ref played in a loop while present"
        }
      }
    },
    "furniture": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "model": {
          "type": "string",
          "description": "a .nif path (rooted at Meshes\\, omit the prefix), vanilla OR custom"
        },
        "keywords": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          }
        }
      }
    },
    "sound": {
      "type": "object",
      "required": [
        "editorId",
        "files"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "files": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          },
          "description": "Data-relative Sound\\... paths to .wav/.xwm audio (the package step bundles them)"
        },
        "category": {
          "$ref": "#/$defs/ref",
          "description": "SNCT ref; empty -> Skyrim.esm:0x0172A1 AudioCategorySFX"
        },
        "outputModel": {
          "$ref": "#/$defs/ref",
          "description": "SOPM ref; empty -> Skyrim.esm:0x0B4058 vanilla SFX output"
        },
        "priority": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "staticAttenuation": {
          "type": "number",
          "description": "dB attenuation"
        }
      }
    },
    "textureSet": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "TextureSet (TXST): retexture an existing mesh without a new .nif. All slot paths are RELATIVE to Data\\Textures\\ (omit the leading 'Textures\\') and end in .dds. Wire it via a record's alternateTextures.",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "diffuse": {
          "type": "string",
          "description": "slot 0 — color/albedo (_d.dds), relative to Data\\Textures\\"
        },
        "normal": {
          "type": "string",
          "description": "slot 1 — normal + gloss (_n.dds)"
        },
        "mask": {
          "type": "string",
          "description": "slot 2 — environment-mask / subsurface tint"
        },
        "glow": {
          "type": "string",
          "description": "slot 3 — glow / detail map"
        },
        "height": {
          "type": "string",
          "description": "slot 4 — parallax height"
        },
        "environment": {
          "type": "string",
          "description": "slot 5 — environment cube map"
        },
        "multilayer": {
          "type": "string",
          "description": "slot 6 — multilayer"
        },
        "backlight": {
          "type": "string",
          "description": "slot 7 — backlight-mask / specular"
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "NoSpecularMap",
              "FaceGenTextures",
              "HasModelSpaceNormalMap"
            ]
          }
        }
      }
    },
    "alternateTexture": {
      "type": "object",
      "required": [
        "name",
        "textureSet"
      ],
      "description": "Override one named material/sub-mesh of a base .nif with a TXST.",
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "the material/3D-name inside the .nif (e.g. 'NorRubblePiece03:0')"
        },
        "index": {
          "type": "integer",
          "minimum": 0,
          "description": "3D sub-mesh index (default 0)"
        },
        "textureSet": {
          "$ref": "#/$defs/ref",
          "description": "ref → a TXST (in-spec editorId or <master>:0xFORMID)"
        }
      }
    },
    "color": {
      "type": "object",
      "description": "An 8-bit RGB(A) colour; components 0–255 (alpha usually 0 for weather colours).",
      "properties": {
        "r": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "g": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "b": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "a": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        }
      }
    },
    "weatherColor": {
      "type": "object",
      "description": "A weather colour across times of day; omitted ones fall back to `day`.",
      "properties": {
        "sunrise": {
          "$ref": "#/$defs/color"
        },
        "day": {
          "$ref": "#/$defs/color"
        },
        "sunset": {
          "$ref": "#/$defs/color"
        },
        "night": {
          "$ref": "#/$defs/color"
        }
      }
    },
    "cloudLayer": {
      "type": "object",
      "properties": {
        "index": {
          "type": "integer",
          "minimum": 0,
          "maximum": 31,
          "description": "cloud-layer slot 0..31"
        },
        "texture": {
          "type": "string",
          "description": "Textures-relative .dds path, e.g. 'Sky\\\\SkyrimCloudsUpper04.dds'"
        },
        "xSpeed": {
          "type": "number"
        },
        "ySpeed": {
          "type": "number"
        },
        "colors": {
          "$ref": "#/$defs/weatherColor"
        },
        "alphaSunrise": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "alphaDay": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "alphaSunset": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "alphaNight": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        }
      }
    },
    "weather": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "A custom sky (WTHR). Minimal = just editorId (clear-day baseline).",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "template": {
          "$ref": "#/$defs/ref",
          "description": "optional vanilla weather to DeepCopy as base (e.g. Skyrim.esm:0x10E1F2 = SkyrimClear_A); inherited clouds/colours are kept unless overridden"
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "Pleasant",
              "Cloudy",
              "Rainy",
              "Snow",
              "SkyStaticsAlwaysVisible",
              "SkyStaticsFollowsSunPosition"
            ]
          }
        },
        "skyUpperColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "skyLowerColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "fogNearColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "fogFarColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "horizonColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "cloudColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "sunColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "sunlightColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "ambientColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "starsColor": {
          "$ref": "#/$defs/weatherColor"
        },
        "clouds": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/cloudLayer"
          }
        },
        "precipitation": {
          "$ref": "#/$defs/ref",
          "description": "a shader-particle-geometry (SPGD) ref, e.g. Skyrim.esm:0x10780F (rain)"
        },
        "windSpeed": {
          "type": "number",
          "description": "0..1 fraction (or 0..100 percent)"
        },
        "windDirection": {
          "type": "number",
          "minimum": 0,
          "maximum": 360,
          "description": "degrees"
        },
        "windDirectionRange": {
          "type": "number",
          "minimum": 0,
          "maximum": 360
        },
        "fogDayNear": {
          "type": "number"
        },
        "fogDayFar": {
          "type": "number"
        },
        "fogNightNear": {
          "type": "number"
        },
        "fogNightFar": {
          "type": "number"
        },
        "transitionDelta": {
          "type": "number"
        },
        "imageSpaces": {
          "$ref": "#/$defs/weatherImageSpaces",
          "description": "Per-time-of-day ImageSpace (IMGS) for outdoor color grading (HDR/bloom/saturation). Each value is an in-spec imageSpaces editorId or vanilla '<master>:0xFORMID'. `default` fills any time-of-day left empty."
        }
      }
    },
    "weatherImageSpaces": {
      "type": "object",
      "description": "Per-time-of-day IMGS refs for a weather record. `default` fills any unset time-of-day slot.",
      "properties": {
        "default": {
          "type": "string",
          "description": "IMGS ref used for any time-of-day not explicitly set; in-spec editorId or '<master>:0xFORMID'"
        },
        "sunrise": {
          "type": "string",
          "description": "IMGS ref for sunrise; in-spec editorId or '<master>:0xFORMID'"
        },
        "day": {
          "type": "string",
          "description": "IMGS ref for day; in-spec editorId or '<master>:0xFORMID'"
        },
        "sunset": {
          "type": "string",
          "description": "IMGS ref for sunset; in-spec editorId or '<master>:0xFORMID'"
        },
        "night": {
          "type": "string",
          "description": "IMGS ref for night; in-spec editorId or '<master>:0xFORMID'"
        }
      }
    },
    "weatherChance": {
      "type": "object",
      "required": [
        "weather"
      ],
      "properties": {
        "weather": {
          "$ref": "#/$defs/ref",
          "description": "a WTHR ref (in-spec editorId or <master>:0xFORMID)"
        },
        "chance": {
          "type": "integer",
          "minimum": 0,
          "description": "relative weight"
        }
      }
    },
    "climate": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "A weather cycle (CLMT). Minimal = editorId + at least one weather.",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "weathers": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/weatherChance"
          }
        },
        "sunriseBegin": {
          "type": "string",
          "description": "'HH:MM' 24-hour"
        },
        "sunriseEnd": {
          "type": "string",
          "description": "'HH:MM' 24-hour"
        },
        "sunsetBegin": {
          "type": "string",
          "description": "'HH:MM' 24-hour"
        },
        "sunsetEnd": {
          "type": "string",
          "description": "'HH:MM' 24-hour"
        },
        "sunTexture": {
          "type": "string"
        },
        "sunGlareTexture": {
          "type": "string"
        },
        "moons": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "Masser",
              "Secunda"
            ]
          }
        },
        "phaseLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "volatility": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        }
      }
    },
    "worldspace": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "climate": {
          "$ref": "#/$defs/ref",
          "description": "CLMT ref — the sky/lighting cycle (strongly advised; without it the world has no day/night)"
        },
        "water": {
          "$ref": "#/$defs/ref",
          "description": "WATR ref (optional)"
        },
        "lodWater": {
          "$ref": "#/$defs/ref",
          "description": "WATR ref for distant LOD water (optional)"
        },
        "parent": {
          "$ref": "#/$defs/ref",
          "description": "parent WRLD ref (optional)"
        },
        "interiorLighting": {
          "$ref": "#/$defs/ref"
        },
        "location": {
          "$ref": "#/$defs/ref"
        },
        "music": {
          "$ref": "#/$defs/ref"
        },
        "encounterZone": {
          "$ref": "#/$defs/ref"
        },
        "baseTexture": {
          "$ref": "#/$defs/ref",
          "description": "LTEX ref — single ground texture applied as the BASE layer (BTXT) of every cell's LAND, all 4 quadrants. No per-vertex blend; omit = untextured. Works with cells or heightmap."
        },
        "textureLayers": {
          "type": "array",
          "description": "Additional alpha-blended terrain textures stacked on top of baseTexture. Each entry = an LTEX + a grayscale splatmap PNG; non-zero pixels become per-vertex VTXT alpha. Order = stacking order (later entries draw on top).",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["texture", "splatmap"],
            "properties": {
              "texture": {
                "$ref": "#/$defs/ref",
                "description": "LTEX ref for this alpha layer."
              },
              "splatmap": {
                "type": "object",
                "additionalProperties": false,
                "required": ["path"],
                "description": "8-bit grayscale PNG; pixel 0..255 -> per-vertex alpha 0..1 for this texture. Same grid rule as heightmap (width N*32+1, height M*32+1, shared edge columns).",
                "properties": {
                  "path": { "type": "string", "description": "PNG path, relative to the spec file." },
                  "originX": { "type": "integer", "description": "Cell X the PNG's bottom-left (SW) pixel maps to; align with the heightmap/cell grid." },
                  "originY": { "type": "integer", "description": "Cell Y the PNG's bottom-left (SW) pixel maps to." }
                }
              }
            }
          }
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "SmallWorld",
              "CannotFastTravel",
              "NoLodWater",
              "NoLandscape",
              "NoSky",
              "FixedDimensions",
              "NoGrass"
            ]
          }
        },
        "defaultLandHeight": {
          "type": "number",
          "description": "WRLD land default (Tamriel -27000); leave it unless you know better"
        },
        "defaultWaterHeight": {
          "type": "number",
          "description": "WRLD water default (Tamriel -14000); a 0 here floods sub-0 terrain"
        },
        "map": {
          "type": "object",
          "properties": {
            "northwestX": {
              "type": "integer"
            },
            "northwestY": {
              "type": "integer"
            },
            "southeastX": {
              "type": "integer"
            },
            "southeastY": {
              "type": "integer"
            },
            "usableWidth": {
              "type": "integer"
            },
            "usableHeight": {
              "type": "integer"
            },
            "cameraInitialPitch": {
              "type": "number"
            },
            "cameraMinHeight": {
              "type": "number"
            },
            "cameraMaxHeight": {
              "type": "number"
            }
          },
          "additionalProperties": false
        },
        "cells": {
          "type": "array",
          "description": "Flat terrain cells (CELL+LAND) to generate inside this worldspace. Enter in-game with: cow <editorId> X Y",
          "items": {
            "$ref": "#/$defs/worldspaceCell"
          }
        },
        "heightmap": {
          "type": "object",
          "description": "16-bit grayscale PNG heightmap (N×32+1 × M×32+1 px). ModForge derives the cell grid from PNG size and encodes each cell's VHGT. Mutually exclusive with `cells`; if both are set, heightmap wins.",
          "properties": {
            "path": { "type": "string", "description": "PNG path relative to this spec file" },
            "originX": { "type": "integer", "description": "cell-grid X of the PNG's west edge (left column)" },
            "originY": { "type": "integer", "description": "cell-grid Y of the PNG's south edge (bottom row)" },
            "minHeight": { "type": "number", "description": "game units mapped to PNG pixel 0 (black)" },
            "maxHeight": { "type": "number", "description": "game units mapped to PNG pixel 65535 (white)", "default": 4000 }
          },
          "required": ["path"],
          "additionalProperties": false
        },
        "godotPlacements": {
          "type": "object",
          "description": "Godot worldspace-editor placements JSON (exported from the godot-worldspace-editor front-end). Coordinate system 'godot4_y_up' is converted to Skyrim world coords and merged into the placements pipeline. Use alongside `heightmap`; set originX/Y to match.",
          "properties": {
            "path": { "type": "string", "description": "placements.json path relative to this spec file" },
            "originX": { "type": "integer", "description": "cell-grid X of Godot scene's west/south origin (must match heightmap originX)" },
            "originY": { "type": "integer", "description": "cell-grid Y of Godot scene's west/south origin (must match heightmap originY)" }
          },
          "required": ["path"],
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "region": {
      "type": "object",
      "required": [
        "editorId",
        "worldspace"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "worldspace": {
          "$ref": "#/$defs/ref",
          "description": "the WRLD this region lives in (in-spec or vanilla)"
        },
        "edgeFallOff": {
          "type": "integer",
          "minimum": 0,
          "description": "blend distance (units) at the region border"
        },
        "mapColor": {
          "type": "string",
          "description": "world-map color as 0xRRGGBB (optional)"
        },
        "weatherPriority": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "weather": {
          "type": "array",
          "description": "the weather table — >=1 entry, chances must sum > 0 (the climate hook)",
          "items": {
            "type": "object",
            "required": [
              "weather"
            ],
            "properties": {
              "weather": {
                "$ref": "#/$defs/ref",
                "description": "WTHR ref"
              },
              "chance": {
                "type": "integer",
                "minimum": 0,
                "description": "relative weight 0-100"
              },
              "global": {
                "$ref": "#/$defs/ref"
              }
            },
            "additionalProperties": false
          }
        },
        "area": {
          "type": "array",
          "description": "polygon of >=3 world-space points enclosing the area",
          "items": {
            "type": "object",
            "properties": {
              "x": {
                "type": "number"
              },
              "y": {
                "type": "number"
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "worldspaceCell": {
      "type": "object",
      "description": "A flat terrain cell (CELL+LAND) at exterior grid coordinates (X,Y). 1 unit = 4096 game units; (0,0) is origin.",
      "properties": {
        "x": {
          "type": "integer",
          "description": "exterior cell-grid X coordinate"
        },
        "y": {
          "type": "integer",
          "description": "exterior cell-grid Y coordinate"
        },
        "height": {
          "type": "number",
          "default": 4000,
          "description": "terrain height in game units (default 4000 ≈ 280 m above sea level; 0 = sea level)"
        },
        "navmesh": {
          "type": "boolean",
          "description": "generate a flat 4-vertex quad NAVM covering the full cell so NPCs can path on it (no cross-cell links)"
        }
      }
    },
    "mapMarker": {
      "type": "object",
      "required": [
        "editorId",
        "worldspace"
      ],
      "description": "A permanent world-map marker (XMRK on the MapMarker static). Also usable as an objective target via a forced: alias.",
      "properties": {
        "editorId": {
          "type": "string"
        },
        "name": {
          "type": "string",
          "description": "map label"
        },
        "worldspace": {
          "type": "string",
          "description": "exterior worldspace ref <master>:0xFORMID (Tamriel = Skyrim.esm:0x00003C)"
        },
        "position": {
          "$ref": "#/$defs/vec3"
        },
        "type": {
          "type": "string",
          "description": "MapMarker.MarkerType name: City/Town/Settlement/Cave/Camp/Fort/Landmark/… (None if empty)"
        },
        "flags": {
          "type": "array",
          "items": {
            "enum": [
              "Visible",
              "CanTravelTo",
              "ShowAllIsHidden"
            ]
          },
          "description": "empty = hidden until discovered"
        }
      }
    },
    "condition": {
      "type": "object",
      "description": "A CTDA condition (the shared ConditionSpec). function is a supported condition-function name; comparison + value are the test; param/actorValue/itemType supply the function's parameter; runOn picks Subject/Target; or ORs with the next.",
      "required": [
        "function"
      ],
      "properties": {
        "function": {
          "enum": [
            "HasPerk",
            "GetInFaction",
            "GetItemCount",
            "GetGlobalValue",
            "GetStage",
            "GetIsID",
            "GetIsRace",
            "HasKeyword",
            "WornHasKeyword",
            "IsSpellTarget",
            "GetRelationshipRank",
            "GetActorValue",
            "GetActorValuePercent",
            "GetBaseActorValue",
            "GetEquippedItemType",
            "GetCurrentTime",
            "IsInInterior",
            "IsInCombat",
            "GetRandomPercent",
            "GetLevel",
            "TemperIsEnchanted",
            "GetQuestCompleted",
            "GetDistance",
            "GetIsCurrentPackage",
            "GetIsVoiceType",
            "GetIsAliasRef",
            "GetQuestRunning",
            "GetInCell",
            "GetInWorldspace",
            "GetEquipped",
            "GetDeadCount",
            "GetSitting",
            "GetGold",
            "GetMapMarkerVisible",
            "GetStageDone",
            "GetInCurrentLoc",
            "IsSceneActionComplete",
            "GetVMQuestVariable",
            "GetVMScriptVariable"
          ]
        },
        "comparison": {
          "type": "string",
          "description": "== != > >= < <= or a CompareOperator enum name (default >=)"
        },
        "value": {
          "type": "number"
        },
        "param": {
          "$ref": "#/$defs/ref",
          "description": "FormLink param (perk/keyword/npc/race/item/magicItem/faction/global/quest)"
        },
        "actorValue": {
          "type": "string",
          "description": "ActorValue param for GetBaseActorValue/GetActorValue (e.g. OneHanded, Destruction)"
        },
        "alias": {
          "type": "string",
          "description": "for GetIsAliasRef: an alias NAME on the owning quest (dialogue/banter/scene/stage/objective only) — resolved to its alias index"
        },
        "stage": {
          "type": "integer",
          "description": "for GetStageDone: the stage INDEX to test (a parameter, distinct from the comparison value)"
        },
        "variableName": {
          "type": "string",
          "description": "for GetVMQuestVariable/GetVMScriptVariable: the Papyrus property name read off the attached script (param = the quest / the object)"
        },
        "scene": {
          "type": "string",
          "description": "for IsSceneActionComplete: the scene whose action is tested (a scene editorId ref; omit on a scene condition to default to the owning scene)"
        },
        "sceneActionIndex": {
          "type": "integer",
          "description": "for IsSceneActionComplete: the action's index in the built SCEN (inspect with scenediag)"
        },
        "itemType": {
          "enum": [
            "Left",
            "Right",
            "Voice",
            "Instant"
          ],
          "description": "CastSource for GetEquippedItemType"
        },
        "runOn": {
          "enum": [
            "Subject",
            "Target",
            "Reference",
            "CombatTarget",
            "LinkedReference",
            "QuestAlias",
            "PackageData",
            "EventData"
          ],
          "default": "Subject"
        },
        "reference": {
          "$ref": "#/$defs/ref",
          "description": "the ref read when runOn=Reference (e.g. the player Skyrim.esm:0x000014)"
        },
        "or": {
          "type": "boolean",
          "default": false
        }
      }
    },
    "perkEffect": {
      "type": "object",
      "description": "One perk effect. kind=ability grants spell; kind=entryPoint modifies a named EntryPoint; kind=addActivateChoice adds an '[E] <buttonLabel>' choice (spell and/or fragmentBody); kind=setText changes the activation prompt text.",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "enum": [
            "ability",
            "entryPoint",
            "addActivateChoice",
            "setText"
          ]
        },
        "rank": {
          "type": "integer",
          "minimum": 0,
          "description": "applies at perk rank >= this (default 0)"
        },
        "priority": {
          "type": "integer",
          "minimum": 0,
          "description": "tie-break ordering among effects"
        },
        "conditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/condition"
          },
          "description": "effect-level CTDA gates"
        },
        "spell": {
          "$ref": "#/$defs/ref",
          "description": "(ability) SPEL ref to grant"
        },
        "entryPoint": {
          "type": "string",
          "description": "(entryPoint) EntryType name, e.g. ModAttackDamage. List all: perkdiag <Skyrim.esm> entrypoints"
        },
        "function": {
          "enum": [
            "Set",
            "Add",
            "Multiply",
            "Mult"
          ],
          "default": "Multiply",
          "description": "(entryPoint) how value is applied"
        },
        "value": {
          "type": "number",
          "description": "(entryPoint) the operand, e.g. 1.2 for +20% with Multiply"
        },
        "buttonLabel": {
          "type": "string",
          "description": "(addActivateChoice/setText) the '[E] <label>' choice prompt"
        },
        "text": {
          "type": "string",
          "description": "(setText) the new activation prompt text"
        },
        "replaceDefault": {
          "type": "boolean",
          "description": "(addActivateChoice) replace the default activation instead of adding a choice"
        },
        "fragmentBody": {
          "type": "string",
          "description": "(addActivateChoice) raw Papyrus run when the choice is picked; akTargetRef (the activated object) + akActor (the activator) are in scope. Triggers PerkAdapter fragment generation (package ships <perk>_Frags.pex)."
        }
      }
    },
    "perk": {
      "type": "object",
      "required": [
        "editorId",
        "name"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "description": {
          "type": "string"
        },
        "playable": {
          "type": "boolean",
          "default": true
        },
        "hidden": {
          "type": "boolean",
          "default": false
        },
        "trait": {
          "type": "boolean",
          "default": false
        },
        "level": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "numRanks": {
          "type": "integer",
          "minimum": 1,
          "maximum": 255,
          "default": 1
        },
        "nextPerk": {
          "$ref": "#/$defs/ref",
          "description": "optional ref to the next perk in a rank chain"
        },
        "conditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/condition"
          },
          "description": "perk-level CTDA gates"
        },
        "effects": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/perkEffect"
          }
        }
      }
    },
    "wordOfPower": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "description": "dragon-script glyph shown in the shout menu"
        },
        "translation": {
          "type": "string",
          "description": "English gloss"
        }
      }
    },
    "shoutWord": {
      "type": "object",
      "properties": {
        "word": {
          "$ref": "#/$defs/ref",
          "description": "WOOP ref"
        },
        "spell": {
          "$ref": "#/$defs/ref",
          "description": "SPEL ref — a Voice-type spell fired at this charge level"
        },
        "recoveryTime": {
          "type": "number",
          "description": "cooldown in seconds"
        }
      }
    },
    "shout": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "menuDisplayObject": {
          "$ref": "#/$defs/ref"
        },
        "words": {
          "type": "array",
          "maxItems": 3,
          "items": {
            "$ref": "#/$defs/shoutWord"
          }
        }
      }
    },
    "wordWall": {
      "type": "object",
      "required": [
        "editorId",
        "shout"
      ],
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1,
          "description": "editorId of the generated teaching quest"
        },
        "name": {
          "type": "string"
        },
        "shout": {
          "$ref": "#/$defs/ref",
          "description": "SHOU ref the wall teaches"
        },
        "wordIndex": {
          "type": "integer",
          "minimum": 1,
          "maximum": 3,
          "description": "which of the shout's 3 words to teach"
        },
        "word": {
          "$ref": "#/$defs/ref",
          "description": "explicit WOOP ref; auto-derived from an in-spec shout's wordIndex, required for a vanilla shout"
        },
        "scriptName": {
          "type": "string",
          "description": "generated fragment name; defaults to <editorId>Script"
        },
        "triggerEditorId": {
          "type": "string"
        },
        "triggerBase": {
          "$ref": "#/$defs/ref",
          "description": "ACTI base to place; defaults to vanilla WordWallTrigger Skyrim.esm:0x05095E"
        },
        "cell": {
          "type": "string",
          "description": "interior: in-spec editorId or <master>:0xFORMID"
        },
        "worldspace": {
          "type": "string",
          "description": "exterior worldspace ref; position is world-space"
        },
        "position": {
          "$ref": "#/$defs/vec3"
        },
        "rotation": {
          "$ref": "#/$defs/vec3"
        }
      }
    },
    "npcItem": {
      "type": "object",
      "required": [
        "item"
      ],
      "description": "One inventory entry on an NPC: a carriable item ref + count.",
      "properties": {
        "item": {
          "$ref": "#/$defs/ref",
          "description": "ref → a carriable item (weapon/armor/misc/potion/ammo/gold). Gold001 = Skyrim.esm:0x00000F"
        },
        "count": {
          "type": "integer",
          "description": "stack count (default 1; use larger counts for gold/arrows/potions; must be != 0)"
        }
      }
    },
    "banter": {
      "type": "object",
      "required": [
        "questEditorId"
      ],
      "description": "A proactive unprompted NPC line (ambient IDLE topic). Only surfaces while the NPC has idle chatter (an AI package with the AllowIdleChatter interrupt flag). Use conditions to make it situational.",
      "properties": {
        "editorId": {
          "type": "string",
          "description": "optional — names the INFO group for diag/uniqueness"
        },
        "questEditorId": {
          "type": "string",
          "minLength": 1,
          "description": "host quest editorId (must be StartGameEnabled, like dialogue)"
        },
        "speakerNpcEditorId": {
          "type": "string",
          "description": "who says it (auto GetIsID gate); an npc editorId"
        },
        "responses": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "the spoken line(s) for this one comment (played in sequence)"
        },
        "emotion": {
          "enum": [
            "Neutral",
            "Anger",
            "Disgust",
            "Fear",
            "Sad",
            "Happy",
            "Surprise",
            "Puzzled"
          ]
        },
        "emotionValue": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100
        },
        "conditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/condition"
          },
          "description": "situational gates beyond the auto speaker gate (GetCurrentTime, IsInInterior, GetActorValuePercent, a follower-faction gate, …)"
        }
      }
    },
    "scene": {
      "type": "object",
      "required": [
        "editorId",
        "questEditorId"
      ],
      "description": "SCEN — NPCs talking to EACH OTHER (not the player), hosted by a quest, played as ordered phases. The participant NPCs must be PLACED near each other (a placements[] entry per NPC into the same cell) for the conversation to fire in-game.",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "questEditorId": {
          "type": "string",
          "minLength": 1,
          "description": "host quest editorId (must exist in spec; StartGameEnabled so the scene runs)"
        },
        "actors": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/sceneActor"
          }
        },
        "phases": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/scenePhase"
          }
        },
        "beginOnQuestStart": {
          "type": "boolean",
          "default": true,
          "description": "auto-play the moment the host quest starts (the simplest 'two NPCs chat on load' trigger). Turn off to trigger from a script/package, or use autoStart for presence-gating"
        },
        "stopQuestOnEnd": {
          "type": "boolean",
          "description": "stop the host quest when the scene finishes (vanilla one-shot conversation scenes set this with beginOnQuestStart)"
        },
        "autoStart": {
          "$ref": "#/$defs/sceneAutoStart",
          "description": "presence-gated auto-start (follower-banter style); when set the scene does NOT auto-play on quest start"
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/sceneAction"
          },
          "description": "non-dialog beats (movement via a package / timed pauses) interleaved with the spoken phases"
        },
        "conditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/condition"
          },
          "description": "scene-level CTDA gate — the scene only STARTS if all pass. ONLY gates a beginOnQuestStart scene; for an autoStart scene use autoStart.gateGlobal (the controller forces Scene.Start(), bypassing scene begin-conditions)"
        }
      }
    },
    "sceneActor": {
      "type": "object",
      "required": [
        "aliasId",
        "npc"
      ],
      "description": "One scene participant: an alias INDEX (unique in the host quest) + the NPC that fills it (UniqueActor-bound).",
      "properties": {
        "aliasId": {
          "type": "integer",
          "minimum": 0,
          "description": "alias index in the host quest (unique, >=0); the SceneActor references this"
        },
        "npc": {
          "$ref": "#/$defs/ref",
          "description": "ref → the NPC that fills this alias (in-spec editorId or <master>:0xFORMID)"
        },
        "name": {
          "type": "string",
          "description": "optional alias name (defaults to the npc editorId)"
        }
      }
    },
    "scenePhase": {
      "type": "object",
      "description": "One scene phase: actor `speaker` says `lines` with emotion; phases play in list order. A phase referenced only by an action may have empty lines (a pure beat).",
      "properties": {
        "speaker": {
          "type": "integer",
          "description": "the aliasId (from actors[]) who speaks this phase"
        },
        "lines": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "the spoken line(s) for this phase (played in sequence)"
        },
        "emotion": {
          "enum": [
            "Neutral",
            "Anger",
            "Disgust",
            "Fear",
            "Sad",
            "Happy",
            "Surprise",
            "Puzzled"
          ]
        },
        "emotionValue": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100
        },
        "headtrackActor": {
          "type": "integer",
          "default": -2,
          "description": "actor aliasId to look at; -1 = look at no one; -2 (default) = the OTHER actor"
        },
        "headtrackPlayer": {
          "type": "boolean",
          "description": "speaker headtracks the PLAYER (mutually exclusive with a non-default headtrackActor)"
        },
        "faceTarget": {
          "type": "boolean",
          "description": "whether the FaceTarget flag is set (default true if unset)"
        },
        "startConditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/condition"
          },
          "description": "the phase only PLAYS if all pass (else the engine skips to the next phase)"
        },
        "completionConditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/condition"
          },
          "description": "the phase ENDS once all pass (a condition-driven advance beyond 'advance when the line finishes')"
        }
      }
    },
    "sceneAction": {
      "type": "object",
      "description": "A non-dialog scene beat over a phase window. Set `idle` (PlayIdle via a phase fragment — may add `timerSeconds` as the pose-hold), `package` (a Package action), or `timerSeconds` > 0 (a Timer action). idle/package are mutually exclusive; package/timerSeconds are mutually exclusive.",
      "properties": {
        "actor": {
          "type": "integer",
          "description": "aliasId (from actors[]) that performs the action"
        },
        "idle": {
          "$ref": "#/$defs/ref",
          "description": "ref → an IDLE record; non-empty makes this a PlayIdle action: `actor` plays the idle when `startPhase` begins (via a SceneAdapter phase fragment + an auto-emitted Timer to run/hold the phase), then returns to AI. Only idles vanilla scripts actually PlayIdle work (bow/gesture/offset); furniture-gated kneel/pray idles do nothing"
        },
        "package": {
          "$ref": "#/$defs/ref",
          "description": "ref → a PACK (a packages[] editorId or <master>:0xFORMID) the actor runs across the phase window (Travel = walk to a marker; Sandbox = ambient activity)"
        },
        "timerSeconds": {
          "type": "number",
          "description": "> 0 → a Timer action: the scene waits this many seconds over the phase window (no actor behaviour)"
        },
        "startPhase": {
          "type": "integer",
          "description": "first phase index (into phases[]) the action spans"
        },
        "endPhase": {
          "type": "integer",
          "default": -1,
          "description": "last phase index; -1 = same as startPhase"
        }
      }
    },
    "sceneAutoStart": {
      "type": "object",
      "description": "Presence-gated auto-start tuning. The MFSceneBanterController polls and calls Scene.Start() when the player is within range of BOTH actors (+ optional LOS, not in combat, neither dead) and the cooldown has elapsed. Distances in game units, times in REAL seconds.",
      "properties": {
        "triggerDistance": {
          "type": "number",
          "default": 2048,
          "description": "max distance from the player to EACH actor"
        },
        "requireLineOfSight": {
          "type": "boolean",
          "description": "also require the player HasLOS both actors"
        },
        "cooldownSeconds": {
          "type": "number",
          "default": 60,
          "description": "min real seconds between plays"
        },
        "pollSeconds": {
          "type": "number",
          "default": 5,
          "description": "RegisterForSingleUpdate interval"
        },
        "brawlOnEnd": {
          "type": "boolean",
          "description": "the two actors fight each other (StartCombat both ways) the moment the dialogue finishes; mark the actors essential for a non-lethal brawl"
        },
        "playOnce": {
          "type": "boolean",
          "description": "play at most once ever (the controller stops polling after the single play)"
        },
        "playHour": {
          "type": "number",
          "default": -1,
          "description": "only play within ±playHourTolerance of this in-game hour (0..24, circular); -1 = any time"
        },
        "playHourTolerance": {
          "type": "number",
          "default": 1,
          "description": "± hours window around playHour"
        },
        "gateGlobal": {
          "$ref": "#/$defs/ref",
          "description": "ref → a GlobalVariable (GLOB) re-arm token: the scene plays only while it == 0; the controller SetValue(1) right after playing; some OTHER generated content SetValue(0) to re-enable it (build the GLOB in globals[])"
        }
      }
    },
    "projectile": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "PROJ — the bolt that flies from caster to target on an Aimed cast; carries its own model + flight physics + the explosion it triggers on impact.",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "type": {
          "enum": [
            "Missile",
            "Lobber",
            "Beam",
            "Flame",
            "Cone",
            "Barrier",
            "Arrow"
          ],
          "default": "Missile",
          "description": "Projectile.TypeEnum — Missile = firebolt-style"
        },
        "speed": {
          "type": "number",
          "default": 3000
        },
        "gravity": {
          "type": "number",
          "default": 0
        },
        "range": {
          "type": "number",
          "default": 12000
        },
        "lifetime": {
          "type": "number",
          "default": 10
        },
        "impactForce": {
          "type": "number",
          "default": 1
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Projectile.Flag names: Explosion / Supersonic / Hitscan / MuzzleFlash / CanBeDisabled / PassThroughSmallTransparent / Rotation / … (default [Explosion])"
        },
        "model": {
          "type": "string",
          "description": ".nif path — VERIFY against vanilla (a wrong path = an INVISIBLE bolt, no error)"
        },
        "light": {
          "$ref": "#/$defs/ref",
          "description": "LIGT ref (in-flight light)"
        },
        "muzzleFlash": {
          "$ref": "#/$defs/ref",
          "description": "LIGT ref (muzzle flash)"
        },
        "sound": {
          "$ref": "#/$defs/ref",
          "description": "SNDR ref (in-flight)"
        },
        "explosion": {
          "$ref": "#/$defs/ref",
          "description": "EXPL ref — the boom on impact (in-spec editorId or <master>:0xFORMID)"
        },
        "collisionRadius": {
          "type": "number"
        },
        "coneSpread": {
          "type": "number"
        }
      }
    },
    "musicTrack": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "MUST — an audio entry. SingleTrack plays `file`; Palette shuffles its sub-`tracks`; SilentTrack is a timed silence.",
      "properties": {
        "editorId": {
          "type": "string"
        },
        "type": {
          "enum": [
            "SingleTrack",
            "Palette",
            "SilentTrack"
          ]
        },
        "file": {
          "type": "string",
          "description": "audio path under Data/Music (.xwm/.wav); a loose asset shipped via package --assets"
        },
        "fadeOut": {
          "type": "number"
        },
        "duration": {
          "type": "number"
        },
        "loopBegins": {
          "type": "number",
          "description": "loop start seconds; -1 (default) = no loop"
        },
        "loopEnds": {
          "type": "number"
        },
        "loopCount": {
          "type": "integer",
          "description": "0 = infinite when looping"
        },
        "tracks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Palette: refs -> other musicTracks editorIds"
        }
      }
    },
    "music": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "MUSC — a music type the game selects by priority and assigns to cells[].music / worldspaces[].music.",
      "properties": {
        "editorId": {
          "type": "string"
        },
        "flags": {
          "type": "array",
          "items": {
            "enum": [
              "PlaysOneSelection",
              "AbruptTransition",
              "CycleTracks",
              "MaintainTrackOrder",
              "DucksCurrentTrack",
              "DoesNotQueue"
            ]
          }
        },
        "priority": {
          "type": "integer",
          "minimum": 0,
          "description": "higher wins over lower-priority music at the same time"
        },
        "duckingDecibel": {
          "type": "number",
          "minimum": 0,
          "maximum": 655,
          "description": "POSITIVE dB attenuation applied to other audio"
        },
        "fadeDuration": {
          "type": "number"
        },
        "tracks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "refs -> musicTracks (MUST) editorIds"
        }
      }
    },
    "hazard": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "HAZD — a radius effect that applies `spell` to actors in it every `targetInterval` seconds. Spawn it with a magicEffect archetype 'SpawnHazard' (+ association = this editorId) or place it (placements[].base = this editorId, or kind:'hazard').",
      "properties": {
        "editorId": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "model": {
          "type": "string",
          "description": ".nif visual path (verify vs Skyrim.esm; empty = invisible)"
        },
        "radius": {
          "type": "number"
        },
        "lifetime": {
          "type": "number",
          "description": "seconds (0 = inherit from spawn spell / permanent)"
        },
        "targetInterval": {
          "type": "number",
          "description": "seconds between applications"
        },
        "limit": {
          "type": "integer",
          "minimum": 0,
          "description": "max simultaneous instances (0 = unlimited)"
        },
        "spell": {
          "type": "string",
          "description": "ref -> SPEL applied periodically"
        },
        "flags": {
          "type": "array",
          "items": {
            "enum": [
              "AffectsPlayerOnly",
              "InheritDurationFromSpawnSpell",
              "AlignToImpactNormal",
              "InheritRadiusFromSpawnSpell",
              "DropToGround"
            ]
          }
        },
        "light": {
          "type": "string"
        },
        "sound": {
          "type": "string"
        },
        "imageSpaceModifier": {
          "type": "string"
        },
        "impactDataSet": {
          "type": "string"
        }
      }
    },
    "explosion": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "EXPL — the impact event a projectile/spell triggers: deals damage in radius, shows model + light, plays sound, swaps imageSpaceModifier, and can apply objectEffect (MGEF) to everything in radius (the AoE).",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "damage": {
          "type": "number",
          "default": 0
        },
        "force": {
          "type": "number",
          "default": 0
        },
        "radius": {
          "type": "number",
          "default": 200
        },
        "isRadius": {
          "type": "number",
          "description": "imagespace blend radius"
        },
        "light": {
          "$ref": "#/$defs/ref",
          "description": "LIGT ref"
        },
        "sound": {
          "$ref": "#/$defs/ref",
          "description": "SNDR ref → Explosion.Sound1"
        },
        "impactDataSet": {
          "$ref": "#/$defs/ref",
          "description": "IPDS ref"
        },
        "imageSpaceModifier": {
          "$ref": "#/$defs/ref",
          "description": "IMAD ref — screen post-process swapped on impact"
        },
        "objectEffect": {
          "$ref": "#/$defs/ref",
          "description": "MGEF ref — the in-radius AoE effect"
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Explosion.Flag names: AlwaysUsesWorldOrientation / KnockDownAlways / IgnoreLosCheck / Chain / …"
        },
        "model": {
          "type": "string",
          "description": ".nif FX art"
        }
      }
    },
    "imageSpaceModifier": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "IMAD — a screen-space post-process applied/removed at runtime (brightness/tint), the way vanilla Night-Eye / cinematic flashes work. Referenced by explosion.imageSpaceModifier or a Papyrus ImageSpaceModifier property (a script applies it via ApplyCrossFade). A single static value (one keyframe per field).",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "brightnessMultiplier": {
          "type": "number",
          "default": 1.6,
          "description": "CinematicBrightnessMult (1 = neutral, >1 brighter)"
        },
        "contrast": {
          "type": "number",
          "default": 1.0,
          "description": "CinematicContrastMult"
        },
        "saturation": {
          "type": "number",
          "default": 1.0,
          "description": "CinematicSaturationMult"
        },
        "tintColor": {
          "$ref": "#/$defs/color",
          "description": "optional screen tint (RGB)"
        },
        "tintAmount": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "tint strength 0..1 (mapped to the colour's alpha)"
        },
        "duration": {
          "type": "number",
          "default": 1.0,
          "description": "fade/animate duration (s)"
        },
        "animatable": {
          "type": "boolean",
          "description": "animate the curves over duration"
        }
      }
    },
    "light": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "LIGT — a custom light source (colour / radius / fade / flicker). Minimal = just editorId (vanilla-sane omnidirectional). Place it via placements[] (base = this editorId).",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "description": "in-game name (only shown for carriable lights like torches)"
        },
        "color": {
          "$ref": "#/$defs/color",
          "description": "RGB tint (0..255 components; alpha unused). Default: warm white"
        },
        "radius": {
          "type": "integer",
          "minimum": 0,
          "default": 256,
          "description": "how far the light reaches (world units)"
        },
        "fadeValue": {
          "type": "number",
          "default": 1.0,
          "description": "brightness fade multiplier"
        },
        "falloffExponent": {
          "type": "number",
          "description": "how quickly brightness drops with distance"
        },
        "fov": {
          "type": "number",
          "description": "field-of-view (degrees) for spotlights"
        },
        "flags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Light.Flag names: Dynamic / Flicker / PortalStrict / CanBeCarried / SpotLight / ShadowSpotlight / OffByDefault / FlickerSlow / Pulse / PulseSlow / SpotShadow"
        },
        "value": {
          "type": "integer",
          "minimum": 0,
          "description": "inventory gold value (carriable lights)"
        },
        "weight": {
          "type": "number",
          "minimum": 0,
          "description": "inventory weight (carriable lights)"
        }
      }
    },
    "identity": {
      "type": "object",
      "required": [
        "id",
        "faction"
      ],
      "description": "A lightweight identity/class the player can hold. `faction` is the persistent held-signal FACT (a bare in-spec editorId is auto-built, or an external <master>:0xID is used as-is). `priority` decides which identity wins as the primary greeting.",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "the tag used by identity/primaryIdentity dialogue gates"
        },
        "faction": {
          "$ref": "#/$defs/ref",
          "description": "the holding FACT — built if it's a bare in-spec editorId, else an external faction ref"
        },
        "priority": {
          "type": "integer",
          "default": 0,
          "description": "higher wins as the primary greeting identity"
        },
        "grants": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          },
          "description": "SPELs/abilities added on join, removed on leave"
        },
        "grantPerks": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ref"
          },
          "description": "PERKs added on join, removed on leave (e.g. a conditional smite-vs-undead perk); the acquire book grants the first, a default identity grants all"
        },
        "toggle": {
          "type": "boolean",
          "default": false,
          "description": "reading the acquire book again removes the identity"
        },
        "default": {
          "type": "boolean",
          "default": false,
          "description": "every player holds this from the start (baseline) — auto-granted by a StartGameEnabled quest (MFIdentityDefault)"
        },
        "activeWhen": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/condition"
          },
          "description": "situational CTDA that NARROW when the identity counts as active (e.g. WornHasKeyword/GetBaseActorValue/GetRelationshipRank/GetGlobalValue for reputation); each runs on the PLAYER unless it sets a non-default runOn"
        },
        "acquireBook": {
          "$ref": "#/$defs/ref",
          "description": "a BOOK whose OnRead grants/toggles this identity (MFIdentityBook)"
        },
        "acquireText": {
          "type": "string",
          "description": "optional yes/no MessageBox prompt shown on read"
        },
        "onAcquire": {
          "type": "object",
          "description": "optional performance played when the identity is acquired",
          "properties": {
            "scene": {
              "type": "string",
              "description": "a scene editorId started on acquire"
            }
          }
        },
        "autoGrantWhen": {
          "type": "object",
          "description": "auto-join this identity's faction once the player's ActorValue crosses a threshold (a poll controller reads GetActorValue; e.g. Dragonborn on DragonSouls>=1). Grants the faction signal only.",
          "properties": {
            "actorValue": {
              "type": "string",
              "description": "the ActorValue name to read (e.g. \"DragonSouls\")"
            },
            "threshold": {
              "type": "number",
              "default": 1,
              "description": "grant once GetActorValue(name) >= this"
            }
          },
          "required": [
            "actorValue"
          ]
        }
      }
    },
    "global": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "GLOB — a named number shared game-wide, save-persisted, readable by conditions (GetGlobalValue) + Papyrus + console. `value` is the INITIAL value only (an existing save keeps its own runtime value). Dialogue `setGlobal` can modify non-constant globals at runtime for flags/reputation counters.",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "type": {
          "enum": [
            "short",
            "long",
            "int",
            "float"
          ],
          "default": "short",
          "description": "Skyrim stores every global as a float on disk; short/long truncate to integer on read. 'int' is an alias for 'long'. Default short (the common flag/counter)"
        },
        "value": {
          "type": "number",
          "description": "initial value"
        },
        "constant": {
          "type": "boolean",
          "description": "Constant major flag — a read-only tuning value (can't be SetValue'd)"
        }
      }
    },
    "ambientColors": {
      "type": "object",
      "description": "Six-direction hemisphere ambient light (DALC) — the flat fill that brightens a dark room overall.",
      "properties": {
        "xPlus": {
          "$ref": "#/$defs/color",
          "description": "+X direction (east)"
        },
        "xMinus": {
          "$ref": "#/$defs/color",
          "description": "-X direction (west)"
        },
        "yPlus": {
          "$ref": "#/$defs/color",
          "description": "+Y direction (north)"
        },
        "yMinus": {
          "$ref": "#/$defs/color",
          "description": "-Y direction (south)"
        },
        "zPlus": {
          "$ref": "#/$defs/color",
          "description": "+Z direction (up/sky)"
        },
        "zMinus": {
          "$ref": "#/$defs/color",
          "description": "-Z direction (down/ground)"
        },
        "specular": {
          "$ref": "#/$defs/color",
          "description": "specular highlight colour"
        },
        "scale": {
          "type": "number",
          "description": "overall DALC brightness multiplier (1.0 = neutral)"
        }
      }
    },
    "lightingTemplate": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "LightingTemplate (LGTM): reusable interior lighting authored by copying a vanilla LGTM then overriding fields. All colour/float fields are optional (null = keep the template value).",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "template": {
          "type": "string",
          "description": "optional vanilla LGTM to DeepCopy as base ('<master>:0xFORMID', e.g. Skyrim.esm:0x0300E2 = DefaultLightingTemplate)"
        },
        "ambientColor": {
          "$ref": "#/$defs/color",
          "description": "flat ambient fill color (RGB 0-255)"
        },
        "directionalColor": {
          "$ref": "#/$defs/color",
          "description": "directional light color (RGB 0-255)"
        },
        "directionalRotationXY": {
          "type": "integer",
          "description": "directional light XY rotation (degrees)"
        },
        "directionalRotationZ": {
          "type": "integer",
          "description": "directional light Z rotation (degrees)"
        },
        "directionalFade": {
          "type": "number",
          "description": "directional light fade multiplier"
        },
        "fogNearColor": {
          "$ref": "#/$defs/color",
          "description": "near fog color"
        },
        "fogFarColor": {
          "$ref": "#/$defs/color",
          "description": "far fog color"
        },
        "fogNear": {
          "type": "number",
          "description": "near fog distance"
        },
        "fogFar": {
          "type": "number",
          "description": "far fog distance"
        },
        "fogMax": {
          "type": "number",
          "description": "maximum fog opacity (0-1)"
        },
        "fogClipDistance": {
          "type": "number",
          "description": "fog clip distance"
        },
        "fogPower": {
          "type": "number",
          "description": "fog power (density curve)"
        },
        "lightFadeStart": {
          "type": "number",
          "description": "distance at which lights start to fade"
        },
        "lightFadeEnd": {
          "type": "number",
          "description": "distance at which lights fully fade"
        },
        "directionalAmbient": {
          "$ref": "#/$defs/ambientColors",
          "description": "DALC six-direction hemisphere fill"
        }
      }
    },
    "imageSpace": {
      "type": "object",
      "required": [
        "editorId"
      ],
      "description": "ImageSpace (IMGS): screen-space HDR / cinematic / tint base record attached to a CELL (distinct from IMAD which is a runtime modifier). Copy a vanilla IMGS via `template` then bump.",
      "properties": {
        "editorId": {
          "type": "string",
          "minLength": 1
        },
        "template": {
          "type": "string",
          "description": "optional vanilla IMGS to DeepCopy as base ('<master>:0xFORMID')"
        },
        "eyeAdaptSpeed": {
          "type": "number",
          "description": "HDR: how fast eye adapts to brightness changes"
        },
        "eyeAdaptStrength": {
          "type": "number",
          "description": "HDR: eye adaptation strength"
        },
        "bloomBlurRadius": {
          "type": "number",
          "description": "HDR: bloom blur radius"
        },
        "bloomThreshold": {
          "type": "number",
          "description": "HDR: brightness threshold before bloom applies"
        },
        "bloomScale": {
          "type": "number",
          "description": "HDR: bloom intensity scale"
        },
        "receiveBloomThreshold": {
          "type": "number",
          "description": "HDR: threshold for receiving bloom"
        },
        "white": {
          "type": "number",
          "description": "HDR: white point (overexposure clamp); >1.0 makes it feel washed-out bright"
        },
        "sunlightScale": {
          "type": "number",
          "description": "HDR: sunlight scale multiplier"
        },
        "skyScale": {
          "type": "number",
          "description": "HDR: sky brightness scale"
        },
        "brightness": {
          "type": "number",
          "description": "cinematic brightness (1.0=neutral, >1 = brighter)"
        },
        "contrast": {
          "type": "number",
          "description": "cinematic contrast (1.0=neutral)"
        },
        "saturation": {
          "type": "number",
          "description": "cinematic saturation (1.0=neutral, >1 = vivid)"
        },
        "tintAmount": {
          "type": "number",
          "description": "tint blend factor (0 = no tint)"
        },
        "tintColor": {
          "$ref": "#/$defs/color",
          "description": "tint color"
        }
      }
    },
    "cellLighting": {
      "type": "object",
      "description": "Inline XCLL per-cell lighting overrides. Fields left null are inherited from the cell's lightingTemplate according to the `inherit` flags.",
      "properties": {
        "ambientColor": {
          "$ref": "#/$defs/color"
        },
        "directionalColor": {
          "$ref": "#/$defs/color"
        },
        "directionalRotationXY": {
          "type": "integer"
        },
        "directionalRotationZ": {
          "type": "integer"
        },
        "directionalFade": {
          "type": "number"
        },
        "fogNearColor": {
          "$ref": "#/$defs/color"
        },
        "fogFarColor": {
          "$ref": "#/$defs/color"
        },
        "fogNear": {
          "type": "number"
        },
        "fogFar": {
          "type": "number"
        },
        "fogMax": {
          "type": "number"
        },
        "fogClipDistance": {
          "type": "number"
        },
        "fogPower": {
          "type": "number"
        },
        "lightFadeBegin": {
          "type": "number",
          "description": "distance at which lights start to fade (XCLL uses Begin/End, LGTM uses Start/End)"
        },
        "lightFadeEnd": {
          "type": "number"
        },
        "directionalAmbient": {
          "$ref": "#/$defs/ambientColors"
        },
        "inherit": {
          "type": "array",
          "items": {
            "enum": [
              "AmbientColor",
              "DirectionalColor",
              "FogColor",
              "FogNear",
              "FogFar",
              "DirectionalRotation",
              "DirectionalFade",
              "ClipDistance",
              "FogPower",
              "FogMax",
              "LightFadeDistances"
            ]
          },
          "description": "field-flag names still inherited from the lightingTemplate (template wins); a field both set inline and listed here is inherited + warned"
        }
      }
    },
    "voiceTemplate": {
      "type": "object",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "engine": {
          "type": "string",
          "enum": [
            "f5",
            "chatterbox",
            "gptsovits",
            "xtts",
            "fish-s2",
            "fish",
            "fishspeech",
            "fish-speech"
          ],
          "default": "f5"
        },
        "referenceWav": {
          "type": "string",
          "description": "path to a zero-shot reference clip (rel. to spec)"
        },
        "referenceText": {
          "type": "string",
          "description": "required transcript for some engines (f5)"
        },
        "modelPath": {
          "type": "string",
          "description": "optional: path to a fine-tuned model directory"
        },
        "rvcModel": {
          "type": "string",
          "description": "optional: path to an RVC model for timbre stabilization"
        },
        "language": {
          "type": "string",
          "default": "en",
          "description": "TTS language hint forwarded to the engine (engines that infer it from the reference ignore this)"
        },
        "seed": {
          "type": "integer",
          "description": "for deterministic output"
        },
        "exaggeration": {
          "type": "number",
          "description": "engine-specific emotion scale; omitted = engine default (f5 ignores it)"
        },
        "speed": {
          "type": "number",
          "description": "playback speed adjustment (e.g. 0.8 = slower); omitted = engine default"
        }
      }
    },
    "voiceLine": {
      "type": "object",
      "properties": {
        "skipLip": {
          "type": "boolean",
          "default": false,
          "description": "true = emit zero-size .lip (static mouth)"
        },
        "format": {
          "type": "string",
          "enum": [
            "fuz",
            "wav",
            "xwm"
          ],
          "default": "fuz"
        }
      }
    },
    "animationReplacer": {
      "type": "object",
      "required": [
        "mod"
      ],
      "description": "OAR replacer-mod: root config {name,author,description} + named submods.",
      "properties": {
        "mod": {
          "type": "string",
          "description": "replacer-mod folder + config name"
        },
        "author": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "submods": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/oarSubmod"
          }
        }
      },
      "additionalProperties": false
    },
    "oarSubmod": {
      "type": "object",
      "required": [
        "name",
        "priority"
      ],
      "description": "One OAR submod: config.json {name,description,priority,conditions[]} + shipped .hkx.",
      "properties": {
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "priority": {
          "type": "integer",
          "description": "higher wins; required >0"
        },
        "replaces": {
          "type": "string",
          "description": "vanilla/MCO anim path being replaced (relative)"
        },
        "hkx": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "user-supplied clips (rel. to Assets/spec dir)"
        },
        "variants": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "optional; emitted under _variants_<anim>/"
        },
        "conditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/oarCondition"
          },
          "description": "empty = applies always"
        },
        "npcMoveset": {
          "$ref": "#/$defs/npcMoveset"
        },
        "replaceVanillaPath": {
          "type": "boolean",
          "description": "true: plain vanilla-path replacer (no config.json/conditions)"
        }
      },
      "additionalProperties": false
    },
    "oarCondition": {
      "type": "object",
      "required": [
        "condition"
      ],
      "description": "One OAR condition (OAR JSON shape, not Skyrim CTDA names). AND/OR nest children in conditions[]. Form refs: 'Plugin.esp|0xFormID'.",
      "properties": {
        "condition": {
          "type": "string",
          "enum": [
            "IsActorBase",
            "IsEquippedType",
            "IsFemale",
            "IsRace",
            "Random",
            "CompareValues",
            "AND",
            "OR"
          ]
        },
        "negated": {
          "type": "boolean"
        },
        "form": {
          "type": "string",
          "description": "IsActorBase/IsRace: 'Plugin.esp|0xFormID'"
        },
        "type": {
          "type": "integer",
          "description": "IsEquippedType weapon-type enum"
        },
        "leftHand": {
          "type": "boolean"
        },
        "graphVariable": {
          "type": "string"
        },
        "graphVariableType": {
          "type": "string",
          "enum": [
            "Int",
            "Float",
            "Bool"
          ]
        },
        "comparison": {
          "type": "string",
          "enum": [
            "==",
            "!=",
            ">",
            ">=",
            "<",
            "<="
          ]
        },
        "value": {
          "type": "number"
        },
        "randomMin": {
          "type": "number"
        },
        "randomMax": {
          "type": "number"
        },
        "conditions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/oarCondition"
          }
        }
      },
      "additionalProperties": false
    },
    "npcMoveset": {
      "type": "object",
      "description": "Sugar: expands to the NPC-moveset condition recipe (IsEquippedType x2 + optional IsActorBase-not-player + IsRace + Random).",
      "properties": {
        "rightWeapon": {
          "type": "string",
          "description": "weapon-type name: fist/sword/dagger/waraxe/mace/greatsword/battleaxe/warhammer/bow/staff/crossbow/shield/torch"
        },
        "leftWeapon": {
          "type": "string"
        },
        "playerOnly": {
          "type": "boolean",
          "description": "false adds IsActorBase not-player(Skyrim.esm|0x7)"
        },
        "race": {
          "type": "string",
          "description": "optional IsRace 'Plugin.esp|0xFormID'"
        },
        "randomPick": {
          "type": "number",
          "description": "optional Random{0..1} < randomPick"
        }
      },
      "additionalProperties": false
    },
    "behaviorData": {
      "type": "object",
      "required": [
        "file"
      ],
      "description": "BDI config (flat array) -> SKSE/Plugins/BehaviorDataInjector/<file>.json.",
      "properties": {
        "file": {
          "type": "string",
          "description": "output filename stem"
        },
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/bdiEntry"
          }
        }
      },
      "additionalProperties": false
    },
    "bdiEntry": {
      "type": "object",
      "required": [
        "type",
        "name"
      ],
      "properties": {
        "projectPath": {
          "type": "string",
          "default": "Actors"
        },
        "type": {
          "type": "string",
          "enum": [
            "kInt",
            "kBool",
            "kFloat",
            "kEvent"
          ]
        },
        "name": {
          "type": "string"
        },
        "value": {
          "type": "number",
          "description": "initial value; omitted in output for kEvent"
        }
      },
      "additionalProperties": false
    },
    "payloadMacro": {
      "type": "object",
      "required": [
        "file"
      ],
      "description": "PIE macro table -> SKSE/PayloadInterpreter/Config/<file>.ini.",
      "properties": {
        "file": {
          "type": "string"
        },
        "section": {
          "type": "string"
        },
        "macros": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/pieMacro"
          }
        }
      },
      "additionalProperties": false
    },
    "pieMacro": {
      "type": "object",
      "required": [
        "name",
        "command"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "macro name (the $ is added on emit)"
        },
        "command": {
          "type": "string",
          "description": "payload command e.g. @SETGHOST|1"
        }
      },
      "additionalProperties": false
    },
    "spidDistribution": {
      "type": "object",
      "required": [
        "file"
      ],
      "description": "SPID config -> <file>_DISTR.ini at the mod folder root (Data/).",
      "properties": {
        "file": {
          "type": "string",
          "description": "output filename stem (SPID requires the _DISTR.ini suffix, added on emit)"
        },
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/spidEntry"
          }
        }
      },
      "additionalProperties": false
    },
    "mcmConfig": {
      "type": "object",
      "required": [
        "modName",
        "pages"
      ],
      "description": "MCM Helper config -> MCM/Config/<modName>/config.json + settings.ini.",
      "properties": {
        "modName": {
          "type": "string",
          "description": "names the MCM/Config/<modName>/ folder + the MCM identity key"
        },
        "displayName": {
          "type": "string",
          "description": "left-list label (supports a $TranslationKey)"
        },
        "pages": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/mcmPage"
          }
        }
      },
      "additionalProperties": false
    },
    "mcmPage": {
      "type": "object",
      "required": [
        "name"
      ],
      "description": "One MCM menu tab (emitted as pageDisplayName).",
      "properties": {
        "name": {
          "type": "string",
          "description": "tab label (supports a $TranslationKey)"
        },
        "cursorFillMode": {
          "enum": [
            "topToBottom",
            "leftToRight"
          ],
          "description": "topToBottom (default) | leftToRight (two-column)"
        },
        "content": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/mcmControl"
          }
        }
      },
      "additionalProperties": false
    },
    "mcmControl": {
      "type": "object",
      "required": [
        "type"
      ],
      "description": "One MCM control. header/empty carry no value; value controls need id + sourceType.",
      "properties": {
        "type": {
          "enum": [
            "toggle",
            "hiddenToggle",
            "slider",
            "stepper",
            "enum",
            "keymap",
            "header",
            "empty"
          ]
        },
        "id": {
          "type": "string",
          "description": "\"key:Section\" — the ini key + [Section] the value is stored under (required for any control with a sourceType)"
        },
        "text": {
          "type": "string",
          "description": "display label (supports $Key and {value})"
        },
        "help": {
          "type": "string",
          "description": "hover tooltip"
        },
        "sourceType": {
          "enum": [
            "ModSettingBool",
            "ModSettingInt",
            "ModSettingFloat",
            "ModSettingString"
          ],
          "description": "ini-backed data source (PropertyValue*/action are out of MVP scope)"
        },
        "min": {
          "type": "number",
          "description": "slider min (a slider needs both min and max)"
        },
        "max": {
          "type": "number",
          "description": "slider max"
        },
        "step": {
          "type": "number"
        },
        "formatString": {
          "type": "string",
          "description": "slider display, e.g. \"{0} s\" (int) / \"{1}\" (float)"
        },
        "options": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "stepper/enum option labels (the int value indexes this)"
        },
        "shortNames": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "enum short display names"
        },
        "defaultBool": {
          "type": "boolean",
          "description": "default for sourceType ModSettingBool"
        },
        "defaultNumber": {
          "type": "number",
          "description": "default for sourceType ModSettingInt/Float"
        },
        "defaultString": {
          "type": "string",
          "description": "default for sourceType ModSettingString"
        },
        "groupControl": {
          "type": "integer",
          "description": "marks this control as a group toggle (an int id)"
        },
        "groupCondition": {
          "type": "integer",
          "description": "show/hide driven by the group toggle with this id"
        },
        "groupConditionNot": {
          "type": "boolean",
          "description": "emit groupCondition as {\"NOT\": id}"
        },
        "groupBehavior": {
          "enum": [
            "disable",
            "skip"
          ],
          "description": "disable (grey out) | skip (hide) the dependent control"
        },
        "position": {
          "type": "integer",
          "description": "two-column forced column: 0 left | 1 right"
        }
      },
      "additionalProperties": false
    },
    "formListInject": {
      "type": "object",
      "required": [
        "file"
      ],
      "description": "FLM config -> <file>_FLM.ini at the mod folder root (Data/). Definitions are emitted before the entries that reference them.",
      "properties": {
        "file": {
          "type": "string",
          "description": "output filename stem (FLM requires the _FLM.ini suffix, added on emit)"
        },
        "filters": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["name", "conditions"],
            "additionalProperties": false,
            "properties": {
              "name": { "type": "string" },
              "conditions": {
                "type": "array",
                "items": { "type": "string" },
                "description": "OR'd: +Plugin.esp (active) / -Plugin.esp (inactive) / +A.esp&-B.esp (AND in one)"
              }
            }
          }
        },
        "aliases": {
          "type": "array",
          "description": "Alias = name|FList,... — bind several target FormLists into one #name.",
          "items": {
            "type": "object",
            "required": ["name", "items"],
            "additionalProperties": false,
            "properties": {
              "name": { "type": "string" },
              "items": { "type": "array", "items": { "type": "string" } }
            }
          }
        },
        "groups": {
          "type": "array",
          "description": "Group = name|Form,... — a reusable form set, referenced as #name in an entry's forms.",
          "items": {
            "type": "object",
            "required": ["name", "items"],
            "additionalProperties": false,
            "properties": {
              "name": { "type": "string" },
              "items": { "type": "array", "items": { "type": "string" } }
            }
          }
        },
        "collections": {
          "type": "array",
          "description": "Collection = name|FormType|keywords|filter — batch-select forms of one FormType with all listed keywords.",
          "items": {
            "type": "object",
            "required": ["name", "formType"],
            "additionalProperties": false,
            "properties": {
              "name": { "type": "string" },
              "formType": {
                "enum": [
                  "Armor", "Weapon", "Ammo", "MagicEffect", "AlchemyItem", "Scroll", "Location",
                  "Ingredient", "Book", "Misc", "Key", "Soulgem", "Activator", "Flora", "Furniture",
                  "Race", "TalkingActivator", "Enchantment", "NPC", "Spell"
                ]
              },
              "keywords": {
                "type": "array",
                "items": { "type": "string" },
                "description": "each keyword required (AND); -keyword excludes"
              },
              "filter": { "type": "string", "description": "optional filter name (a leading # is added if absent)" }
            }
          }
        },
        "entries": {
          "type": "array",
          "description": "FormList = target|forms|filter — the append operations.",
          "items": {
            "type": "object",
            "required": ["target", "forms"],
            "additionalProperties": false,
            "properties": {
              "target": {
                "type": "string",
                "description": "the FormList to append to: EditorID / 0xFormID~Plugin.esp / #Alias"
              },
              "forms": {
                "type": "array",
                "items": { "type": "string" },
                "description": "tokens to add: a form ref / *FormList (expand) / #Group / #Collection"
              },
              "filter": {
                "type": "string",
                "description": "optional filter name (a leading # is added if absent)"
              }
            }
          }
        }
      },
      "additionalProperties": false
    },
    "kidDistribution": {
      "type": "object",
      "required": ["file"],
      "description": "KID config -> <file>_KID.ini at the mod root.",
      "properties": {
        "file": { "type": "string", "description": "output stem (the _KID.ini suffix is added on emit)" },
        "entries": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["keyword", "type"],
            "additionalProperties": false,
            "properties": {
              "keyword": { "type": "string", "description": "the KYWD to distribute (EditorID / 0xFormID~Plugin.esp); unknown EditorID -> KID creates a new KYWD" },
              "type": {
                "enum": ["Weapon", "Armor", "Ammo", "Magic Effect", "Potion", "Scroll", "Location",
                  "Ingredient", "Book", "Misc Item", "Key", "Soul Gem", "Spell", "Activator",
                  "Flora", "Furniture", "Race", "Talking Activator", "Enchantment"]
              },
              "filters": { "type": "array", "items": { "type": "string" }, "description": "name/EditorID/keyword (OR); +x AND, -x exclude, *x wildcard, 0x..~esp formID, *x.nif model" },
              "traits": { "type": "string", "description": "type-specific trait filter, raw passthrough" },
              "chance": { "type": "number", "description": "0.0-100.0 (omit -> 100)" }
            }
          }
        }
      },
      "additionalProperties": false
    },
    "objectSwap": {
      "type": "object",
      "required": ["file"],
      "description": "BOS config -> <file>_SWAP.ini at the mod root.",
      "properties": {
        "file": { "type": "string", "description": "output stem (the _SWAP.ini suffix is added on emit)" },
        "groups": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "conditions": { "type": "array", "items": { "type": "string" }, "description": "[Forms|c1,c2] filter: Location/Region/Keyword/Cell/Worldspace by EditorID/FormID; -x excludes" },
              "entries": {
                "type": "array",
                "items": {
                  "type": "object",
                  "required": ["base", "swaps"],
                  "additionalProperties": false,
                  "properties": {
                    "base": { "type": "string", "description": "base form to replace (0xFormID~Plugin.esp / EditorID)" },
                    "swaps": { "type": "array", "items": { "type": "string" }, "description": "replacement form(s); several -> random pick" },
                    "properties": { "type": "string", "description": "raw transform string, e.g. scale(1.2/1.2),rot(0/0,0/0,45/45)" },
                    "chance": { "type": "number", "description": "0.0-100.0 (omit -> always)" }
                  }
                }
              }
            }
          }
        }
      },
      "additionalProperties": false
    },
    "animObjectSwap": {
      "type": "object",
      "required": ["file"],
      "description": "AOS config -> <file>_ANIO.ini at the mod root.",
      "properties": {
        "file": { "type": "string", "description": "output stem (the _ANIO.ini suffix is added on emit)" },
        "entries": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["base", "swaps"],
            "additionalProperties": false,
            "properties": {
              "base": { "type": "string", "description": "the original ANIO to swap (0xFormID~Plugin.esp / EditorID)" },
              "swaps": { "type": "array", "items": { "type": "string" }, "description": "replacement ANIO(s); several -> random pick per idle" },
              "filters": { "type": "array", "items": { "type": "string" }, "description": "FILTERS header: NPC/Faction/Race/Keyword ref; +x AND, -x exclude, *x string-match" },
              "traits": { "type": "string", "description": "TRAITS header: M/F sex, C/-C child" }
            }
          }
        }
      },
      "additionalProperties": false
    },
    "skyPatcher": {
      "type": "object",
      "required": ["file", "recordType"],
      "description": "SkyPatcher config -> SKSE/Plugins/SkyPatcher/<recordType>/<file>.ini.",
      "properties": {
        "file": { "type": "string", "description": "output stem" },
        "recordType": {
          "enum": ["npc", "armor", "weapon", "ammo", "leveledList", "formList", "race", "container"]
        },
        "patches": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "filters": { "type": "array", "items": { "$ref": "#/$defs/skyPatcherField" }, "description": "filter key=value pairs (all must match)" },
              "mods": { "type": "array", "items": { "$ref": "#/$defs/skyPatcherField" }, "description": "modification key=value pairs (at least one)" }
            }
          }
        }
      },
      "additionalProperties": false
    },
    "skyPatcherField": {
      "type": "object",
      "required": ["key"],
      "additionalProperties": false,
      "properties": {
        "key": { "type": "string" },
        "value": { "type": "string" }
      }
    },
    "spidEntry": {
      "type": "object",
      "required": [
        "type",
        "record"
      ],
      "description": "One distribution line: Type = RecordID|StringFilters|FormFilters|LevelFilters|Traits|TypeParam|Chance. Trailing NONE fields are trimmed on emit.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "Spell",
            "Perk",
            "Item",
            "Shout",
            "LevSpell",
            "Package",
            "Outfit",
            "SleepOutfit",
            "Keyword",
            "DeathItem",
            "Faction",
            "Skin"
          ],
          "description": "distribution type (SleepOutfit/Skin must be explicit — SPID can't infer them)"
        },
        "record": {
          "type": "string",
          "description": "RecordID (field 1, required): '0xFormID~Plugin.esp' or an EditorID. Skyrim/DLC may drop the ~plugin suffix."
        },
        "stringFilters": {
          "type": "array",
          "items": { "type": "string" },
          "description": "field 2: keyword/actorbase EditorID/display name (OR). '-x' excludes, '*x' partial, 'a+b' AND."
        },
        "formFilters": {
          "type": "array",
          "items": { "type": "string" },
          "description": "field 3: Faction/Race/Class/.../FormList FormID|EditorID (OR). '-x' excludes. No wildcards."
        },
        "levelFilters": {
          "type": "string",
          "description": "field 4 (raw): actor range '25/50' / '10/' / '/40', or skill 'SkillIndex(min/max)' e.g. '14(50/100)'."
        },
        "traits": {
          "type": "string",
          "description": "field 5 (raw): letters joined by '/' (M/F/U/S/C/L/T), '-' negates. e.g. 'M/U', '-C'."
        },
        "count": {
          "type": "integer",
          "description": "field 6 for Item: item count (ignored for other types)."
        },
        "packageIndex": {
          "type": "integer",
          "description": "field 6 for Package: package stack insert index (0 = top; ignored for other types)."
        },
        "chance": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "field 7: distribution chance 0-100 (non-unique NPCs only). Omitted -> SPID defaults to 100."
        }
      },
      "additionalProperties": false
    }
  }
}