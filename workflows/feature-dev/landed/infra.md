# 已落地 — infra（voice / spec 解析 / showcase）

← [landed index](README.md)｜對應 [CODE_MAP.infra](../../common/code-map/CODE_MAP.infra.md)

- **MCM → GLOB bridge + configurable perk worked example（offline，2026-08-11）**：`mcmConfigs[].pages[].content[].global` 支援 bool toggle 對 GLOB 的高階綁定；config 產 `CallFunction`，`package` 產/編 per-menu `MCM_ConfigBase` setter，QUST VMAD property 綁同顆 GLOB，perk/任意 CTDA 再用 `GetGlobalValue` gate。`examples/mcm_global_perk.json` 以 `ModBuyPrices` 示範完整鏈；runtime setter 待主力機確認。
- **SQLite/FTS5 plugin catalog MVP（offline，2026-08-11）**：CLI `catalog build <db> <plugin...>` 將 plugin generic records寫入 SQLite（FormKey/plugin/type/EditorID/name）並保存來源 absolute path、SHA-256、localized flag 與 record count；`catalog query` FTS5 搜尋，`catalog get` exact FormKey/override occurrence，`catalog sources` 查 provenance；三個 read command 都可 `--json`。採 temporary DB → replace；record-specific semantic indexing後續擴充。
- **OAR advanced contract（2.2+/3.0，offline，2026-08-11）**：既有 `animationReplacers` 補 condition presets/PRESET、weighted + sequential variants 與 scope、typed functions（CONDITION/RANDOM/ONE/PlaySound）及 activate/deactivate/trigger hooks；保留舊 `variants: string[]` 相容。schema/example/spec/validate/package tests 已同步。

**Voice pipeline（TTS → XWM → FUZ loose asset）**（**in-game 確認 2026-06-13**，真 F5-TTS 克隆 MaleNord 嗓音在自訂 NPC 上實機播出，`ModForgeVoiceTest.zip`）：`voiceTemplates[]` + `npcs[].voiceTemplate` + `voiceLine{format,skipLip}`；CLI `voicelines <spec> <built.esp> [--dry-run|--plan]` 走 built plugin 的 INFO，解析 speaker（GetIsID / GetIsAliasRef / GetInFaction / SceneAction），一個 distinct `voiceType` folder 產一份 voice file；`voicediag <spec> <built.esp>` 離線列 INFO→speaker→voiceType→template→`Sound/Voice/<plugin>/<voiceType>/<quest10>_<topic15>_<infoFormId8>_<n>.fuz`，可在 TTS 前失敗。TTS 透過 `MODFORGE_TTS_BIN`（指 `../skyrim-voicegen/voicegen-f5.sh`——**語音合成已解耦成獨立基石專案**，合約規格見該夾 `PROTOCOL.md`；ModForge 只管包裝）；engine 目前：`f5` 已接、`fish-s2` 轉呼 `MODFORGE_FISH_SPEECH_BIN`（wrapper 必寫 WAV），`chatterbox/gptsovits/xtts` 為保留名。音訊封包：`MODFORGE_XWMAENCODE` 用 Wine 跑 CK `xwmaencode.exe`，`Voice.EncodeXwma` 需 `winepath -w` 轉 Windows path；缺 xWMA 時降級 loose `.wav`，**不把 raw PCM 塞進 `.fuz`**。lip：**首選官方 CK `LipGenerator.exe`（`MODFORGE_LIPGEN`）**——簽名 `<wav> <text> -Language:<lang> -OutputFileName:<lip>`、FonixData.cdf 自 exe 同夾找（免另設 cdf）、本機 `Tools/LipGen/LipGenerator/` 已備、**Wine 實跑產出合法 .lip 確認 2026-06-13**；退化路徑社群 `MODFORGE_FACEFX` + `MODFORGE_FONIXDATA`；皆缺則 no-lip/static mouth（`voicelines` 開頭發一次 warning）。打包：voice files 是 loose assets，不嵌入 ESP/ESM；對最終 mod folder 的 plugin 直接跑 `voicelines`，或把已產出的 `Sound/` 當 `package --assets <dir>` 餵入。**真模型實機跑通的設定（細節見 memory [[voice-gen-interface-future]]）**：① RTX 50 系（Blackwell sm_120）torch 要 **cu128** 不是文檔舊寫的 cu124；② venv 用 python 3.11（3.14 太新）；③ `MODFORGE_TTS_BIN` 被**直接 exec**，故須是「在 f5 venv 內跑 voicegen.py」的 wrapper（`voicegen-f5.sh`）；④ **F5 在 `ref_text=""` 時自動 Whisper 轉寫 ref**，故 vanilla 抽出的 ref 免手寫 transcript（voicegen.py f5 分支已 default ""）；⑤ ref 用 `extract-voices <Voices BSA> MaleNord` 抽 vanilla clip（`examples/refs/` 已 gitignore，vanilla 音檔不 commit）；⑥ xWMAEncode 直接吃 F5 的 24kHz mono PCM，免 resample；⑦ deterministic build 保 INFO FormID 穩定 → `.fuz` 檔名重 package 後不變，仍須 `voicediag` 對 packaged esp 比對 planned vs shipped 再 zip。**踩坑**：空的自訂 CELL（只有 record、無地板 static/無光）會讓放置的 NPC 直接墜落 → 測試 NPC 放真 vanilla 室內、抄一個 vanilla actor 的 z=0 地板座標（Borin 放 `RiverwoodSleepingGiantInn` 0x0133C6、抄 OrgnarREF 0x013486 旁）。lip sync **in-game 確認 2026-06-13**（`ModForgeLipTest.zip`，MF_Smith/Borin 講話時嘴型跟音節動）：官方 CK `LipGenerator.exe`（`MODFORGE_LIPGEN`）Wine 跑通 → `.fuz` 內嵌真 .lip（lipSize>0），跟上次 voice-only 測試（lipSize=0、閉嘴）唯一差別就是這顆 lip。穩流程：先 `package` 到最終資料夾 → 再對該夾內 plugin 跑 `voicelines`（voice 夾名才會 = plugin 名）→ `voicediag` 比 planned vs shipped → flat zip。

**Spec `$ref`/`$env` 解析層**（反序列化前 JSON 預處理器，`SpecRefs.cs`，純 `JsonNode`，全 offline 測試）：任何欄位可放 `$ref`（引另一檔 / 檔#pointer / 同檔 `#/pointer`；值為 **string** | **array 鏈式後蓋前** | **long-form `{from,pointer}`**，`from` 可被 `$env` 驅動）與 `$env`（值 / `default` / 缺報錯）。`$ref` 旁同層 sibling deep-merge **蓋上去（sibling 贏；物件遞迴合併、陣列整個取代）**。CLI `ResolveSpecJson` 單一 chokepoint，`ReadSpec` + `validate` 都在解析後 JSON 上跑（unknown-field 檢查也是）。「具名 preset 庫」= `examples/presets/` 一資料夾 preset 檔（首發 `bright-interior.json`，demo `spec-refs-demo.json`）。**欄位/語意見 `SPEC-refs.md`、wiring 見 `CODE_MAP.infra.md`。** 踩坑：① `$ref` 路徑相對**引用它的那份文件**（preset 檔自己的 `$ref` 相對 preset 檔目錄，非頂層 spec）；② 同層 sibling 蓋 ref、陣列取代不串接；③ `$env` 缺且無 `default` 是**硬報錯**（刻意，無靜默空值）；④ 指令在 deserialize 前就消失，故 `spec.schema.json` 不強制、不衝突它自身的 schema-內 `$ref`。

**templated AI package 必帶模板的 PKCU meta**（IN-GAME 真相 2026-06-22）：一個用 vanilla 程序模板的 PACK，其 `DataInputVersion` + 1-byte `XNAM` marker **必須等於模板自身的值**（vanilla 子包如 WERoad12Travel 完全照抄 Travel 模板的 3/0x05）；缺了引擎**整個忽略 Data input 區** → alias-location/target 槽不解析、演員填了 alias 卻不行動。`PackageTemplates.DataInputMeta` 表（10 模板：sandbox 10/0x20、sleep 6/0x1B、travel 3/0x05、usemagic 1/0x0D、patrol 3/0x09、follow 4/0x09、escort 8/0x12、sittarget 2/0x11、activate 13/0x06、eat 8/0x24，讀自各模板 PACK），`BuildPackageData` dispatch 後套用。先前所有生成 package 都漏設（全 0）→ 同病；經 radiant package 實機定位。測 `PackageAliasTargetTests`。

**一次測 showcase**：`ModForgeShowcase.zip`（批次#1：Light + headtrack + SitTarget）、`ModForgeShowcase2.zip`（批次#2：firebolt PROJ/EXPL + NPC 武器 + scene 閘）。新 diag：`smtree` / `scnscan` / `packagediag` / `lightdiag` / **`identitydiag`（從建好的 esp 還原身份 registry：controller faction↔code、default grants、acquire books、控制 GLOB）** 等。

**動作系統 asset/config 生成（OAR / BDI / PIE，loose files、非-esp）**（**離線實作 + 端到端 package 煙測通過 2026-06-14**；in-game 待測——需 OAR+Pandora+使用者自備 `.hkx`）：三個頂層 spec 區塊 `animationReplacers` / `behaviorData` / `payloadMacros` 生成散裝檔，不碰 Havok、不寫 record。`Spec.AnimationReplacer.cs` DTO；`OarConditions.cs`（`OarConditionSpec`→OAR JSON 的 `Emit`、`npcMoveset`→條件束的 `Expand`、武器型 `WeaponType` enum、`Plugin.esp|0xID`→hex-string 的 `ParseForm`）；`OarGen.cs`（root `{name,author,description}` + 每 submod `{name,description,priority,conditions[]}` 的 `Generate`、`.hkx` 擺放清單 `HkxPlacements`——單 clip+`replaces` 改名為 vanilla basename、`variants`→`_variants_<anim>/`、`replaceVanillaPath`→直擺 vanilla 路徑無 config）；`BdiGen.cs`（BDI flat-array JSON，`kEvent` 省 value）+ `PieGen.Generate`（PIE `.ini` 巨集 `$name = cmd`）；`Generator.Validate.AnimationReplacer.cs`（priority>0、OAR 條件白名單遞迴、武器名/form 解析、BDI type）。`Package.cs` step 7 把產物寫進 MO2 夾、複製使用者自備 hkx（解析自 `assets`/spec 目錄，缺檔回報不靜默丟）、summary 計數。JSON 用 relaxed encoder（`&`/中文字面）。**schema 全實檔驗證**（Holmgang/NAMC/BFCO=OAR、DMK/BFCO=BDI、Stormcloaks=PIE，樣本在 gitignored `../game-data/mods/action-system/`）。28 個離線測試。設計/調查見 [specs/action-system-asset-generation-design.md](../../specs/archive/2026-06-14-action-system-asset-generation-design.md)、[mod-survey/action-system/](../../../../../analysis/mod-survey/action-system/)、[SPEC-animation.md](../../../docs/spec/SPEC-animation.md)。**之後**（roadmap）：variants weight/presets/functions、DAR `_conditions.txt` 後路、`importanim` shell-out（Blender→hkx 牆）、Pandora shell-out。

**SPID `_DISTR.ini` 分發器 config 生成（loose、非-esp）**（**離線實作 + CLI validate 煙測通過 2026-06-17**；roadmap D-1）：頂層 spec 區塊 `spidDistributions` → mod 根 `<file>_DISTR.ini`，讓 mod 無 ESP patch 給**他人/vanilla NPC** attach spell/perk/item/faction/keyword/outfit/package 等（依 string/form/level/trait/chance filter）——follower patch 兼容層標準件。`Spec.SpidDistribution.cs`（`SpidDistributionSpec`/`SpidEntrySpec` DTO）；`SpidGen.cs`（`Generate`→OarFile；`Line` 組單行 `Type = RecordID|StringFilters|FormFilters|LevelFilters|Traits|TypeParam|Chance`，**尾段 NONE 修剪**、中間缺口以 NONE 撐位、`Item`→count/`Package`→index 進第 6 欄）；`Generator.Validate.Spid.cs`（type 白名單 12 種、record 必填不可 NONE、chance 0–100）；`Package.cs` 與 OAR/BDI/PIE 同一 loose-file 段寫出（路徑＝mod 根，≠ `SKSE/Plugins`）。**格式逐欄查 SPID 7.3 reference**（[mod-survey/findings/spid.md](../../../../../analysis/mod-survey/findings/spid.md)，含真實 ini 範例）。**14 離線測**（9 emitter + 5 validate）。docs [SPEC-distribution.md](../../../docs/spec/SPEC-distribution.md)（D 組分發器 config 的家，KID/FLM/SkyPatcher 後續同 pattern）、example `examples/spid_distribution_spec.json`、schema/CODE_MAP.infra 已同步。⚠️ RecordID/EditorID 由玩家 load order 解析，validate 只查結構（離線不驗 form 存在）。**之後**（roadmap D 組）：KID `_KID.ini`、FLM `_FLM.ini`、SkyPatcher、BOS `_SWAP.ini` 等同類 loose-file emitter。

**D 組七個 SKSE loose-ini 分發器全部 IN-GAME CONFIRMED（2026-06-20）**：SPID（上）＋ **MCM Helper**（`mcmConfigs:`）/ **SkyPatcher**（`skyPatchers:`）/ **FLM**（`formListInjects:`）/ **KID**（`kidDistributions:`）/ **BOS**（`objectSwaps:`）/ **AOS**（`animObjectSwaps:`）六個同款 loose-file emitter 全實機收掉，docs 統一在 [SPEC-distribution.md](../../../docs/spec/SPEC-distribution.md)。**MCM Helper 非純 ini**——選單要出現須 ESP 端註冊（Start-Game-Enabled QUST + `ModForgeMCM` extends MCM_ConfigBase + PlayerAlias），且 `MCM/Config/<folder>/` 的資料夾名永遠＝**插件檔名 stem**（非 spec modName），`ModForgeMCM.pex` 需 MCM Helper+SkyUI headers 編。踩坑（FLM 檔首不可有 `[General]` 否則整檔跳過、SkyPatcher NPC 尺寸 key＝`height` 非 `setScale`、MCM 資料夾名）見 memory [[dll-loose-ini-distributors-confirmed]] / [[mcm-helper-registration-recipe]]。runtime 細節與 SKSE log 路徑同上兩 memory。
